# 🎯 PROGRESS — Sistema de Gestión · Escuela de Patín

> **Para Claude Code: leé este archivo completo ANTES de escribir código.**
> Este documento es la fuente de verdad del proyecto. Actualizalo al final de cada módulo terminado (secciones 7 y 8). No avances a un módulo nuevo si el anterior no está marcado ✅, salvo pedido explícito del usuario.

---

## 1. Resumen del proyecto

Sistema tipo Asana/ClickUp simplificado, hecho a medida para una escuela de patín (equipo chico, máx. 10 personas). Reemplaza suscripciones pagas por una herramienta propia.

Es una **PWA** (Progressive Web App): un solo código que funciona en el navegador y también se puede "instalar" en el celular como app, sin pasar por App Store / Play Store.

No maneja carga de archivos, fotos ni audios (fuera de alcance por ahora).

## 2. Roles del sistema

Son 5 niveles de **permiso**, no cargos — varios puestos del club pueden compartir el mismo rol. Para diferenciar a dos personas que comparten rol (ej. dos Profesores con especialidades distintas) existe además el campo `cargo` (texto libre, descriptivo, sin efecto en permisos — ver `docs/roles-actualizacion.md`).

| Rol | Puede |
|---|---|
| **Admin** (dueño) | Crear/editar/borrar todo. Ver todo. Gestionar usuarios y permisos. |
| **Profesor** | Crear y gestionar sus tareas y sus horarios asignados. |
| **Head Coach** | Sobre **tareas**: mismos permisos que Profesor sobre las propias (crear/editar/borrar lo suyo), más lectura de las de Profesor, Empleado y Patinador. Sin edición/borrado de tareas ajenas. Sobre **horarios/turnos**: control total, igual que Admin — crea y reasigna el turno de cualquier profesor (agregado 2026-08-22, ampliado a control total sobre turnos el 2026-08-25 porque es quien arma la mayor parte de las planificaciones de clases). Sin gestión de usuarios. |
| **Empleado** | Ve solo lo asignado a él. Puede cambiar el estado de su propia tarea (Pendiente → En progreso → Completada) y dejar un comentario corto. NO edita estructura, fechas ni asignados. |
| **Patinador** | Mismos permisos que Empleado (agregado 2026-08-22). |

**Mapeo real del equipo → rol y cargo en el sistema** (decidido 2026-08-17, actualizado 2026-08-25 con las cuentas reales cargadas):

| Persona | Email | Rol | Cargo |
|---|---|---|---|
| Lautaro Loza (desarrollador) | `lautaroloza89@gmail.com` | Admin | — |
| Luciana Giacometti (Coordinadora Principal, dueña) | `patinsaintgermain@gmail.com` | Head Coach | Entrenadora Principal |
| Dai (Secretaria: cuotas, pagos, asistencias) | `dai@gmail.com` | Admin | Secretaria |
| Keyla (carga planificaciones) | `keyla@gmail.com` | Profesor | Preparadora Física |
| Carolina | `carolina@gmail.com` | Empleado | Entrenadora Asistente |
| Estefania | `estefania@gmail.com` | Empleado | Ayudante |
| Malena | `malena@gmail.com` | Empleado | Ayudante |

> **Decisión pendiente, no implementada:** el rol de Dai quedó en `Admin` provisoriamente (control total del sistema) porque todavía no existe un rol acotado para el dominio administrativo (cuotas/pagos/asistencias). A futuro se evalúa crear un rol nuevo, análogo a Head Coach pero para esa parte administrativa (permisos amplios sobre lo suyo, sin el control total que hoy le da Admin sobre Tareas/Horarios/gestión de usuarios). No asumir este rol nuevo sin volver a preguntar — todavía no tiene nombre ni alcance definido.

## 3. Stack propuesto (ajustable antes de arrancar la sesión 1)

- **Frontend:** Next.js (React) + TypeScript + Tailwind CSS
- **PWA:** manifest.json + service worker (instalable en mobile)
- **Backend / DB / Auth:** Supabase (Postgres, plan gratuito)
- **Deploy:** Vercel (plan gratuito, auto-deploy conectado a GitHub)

> Si Lauti prefiere otro stack, definirlo antes de empezar — cambia la estructura de carpetas de abajo.

## 4. Estructura de carpetas

```
escuela-patin-app/
├── PROGRESS.md              ← este archivo
├── docs/
│   ├── modelo-datos.md      ← tablas y campos (a definir)
│   ├── roadmap-general.md   ← mapa general (tareas)
│   └── mapa-horarios.md     ← mapa aparte de horarios/turnos
├── public/
│   ├── manifest.json
│   └── icons/
├── src/
│   ├── app/
│   │   ├── (auth)/
│   │   │   └── login/
│   │   ├── (dashboard)/
│   │   │   ├── dashboard/
│   │   │   ├── tareas/
│   │   │   └── horarios/
│   │   └── layout.tsx
│   ├── components/
│   │   ├── ui/               ← botones, inputs, cards genéricos
│   │   ├── tareas/
│   │   ├── horarios/
│   │   └── dashboard/
│   ├── lib/
│   │   ├── supabase/         ← cliente y queries
│   │   └── utils/
│   ├── types/                ← Task, User, Turno, etc.
│   └── hooks/
├── supabase/
│   └── migrations/           ← SQL de tablas
├── .env.local
└── package.json
```

## 5. Orden de construcción (un módulo por sesión)

⚠️ No mezclar módulos en la misma sesión. Terminar, probar, commitear, recién ahí seguir. Esto evita saturar el chat y facilita corregir errores de forma aislada.

| # | Módulo | Depende de | Estado |
|---|---|---|---|
| 1 | Setup base (proyecto, config, deploy vacío) | — | ✅ Terminado |
| 2 | Modelo de datos (tablas en Supabase) | 1 | ✅ Terminado |
| 3 | Auth + roles | 2 | ✅ Terminado |
| 4 | Módulo Tareas | 3 | ✅ Terminado |
| 5 | Módulo Horarios/Turnos | 3 | ✅ Terminado |
| 6 | Dashboard | 4, 5 | ✅ Terminado |
| 7 | Notificaciones | 4, 5 | ✅ Terminado |

Estados: ⬜ Pendiente · 🟡 En progreso · ✅ Terminado

## 6. Reglas de trabajo para Claude Code

1. Al empezar una sesión: leer este archivo completo y ubicar el primer módulo no terminado en la tabla del punto 5.
2. Trabajar SOLO ese módulo. No adelantar código de módulos futuros.
3. Al terminar: actualizar el estado en la tabla y agregar una línea en el "Log de sesiones" (punto 8).
4. No modificar módulos ya marcados ✅ salvo pedido explícito del usuario.
5. Si falta una decisión de producto no definida acá (ej. un campo nuevo en una tabla), preguntar antes de asumir.
6. Al cerrar el módulo, sugerir un mensaje de commit corto y claro.
7. **No reestructurar este documento** (agregar/quitar/fusionar módulos, cambiar el sistema de estados, etc.) sin pedido explícito del usuario. Si un módulo cambia de alcance, avisar en el chat en vez de reescribir la tabla del punto 5.

## 7. Estado actual

**Módulo activo:** ninguno — los 7 módulos de la tabla del punto 5 están ✅ Terminados. Última sesión: **F2 MOD 4 (Asistencia), 2026-09-03** — tabla `asistencia`, navegación grupo → mes → fecha, toma de asistencia en bloque, alerta de 3 semanas sin venir, y el ítem "Asistencia" en la sección Administración del menú. Antes: parche de UI del 2026-09-01 (entrada del módulo Planificaciones unificada en el selector de grupo, filtros de estado movidos a la vista de grupo y mes, "marcar todas como leídas" y contador de sin leer en la campana) — sin cambios de base de datos.
**Próximo paso:** no hay uno definido en este documento. Si surge trabajo nuevo (ej. el rol pendiente para Dai, o algo fuera del alcance original), agregarlo a la tabla del punto 5 solo con pedido explícito del usuario (regla 7 del punto 6).

**Pendientes sueltos (no bloquean nada, pero conviene retomarlos):**
- Confirmar visualmente que Vercel ya redesplegó `214f2a9` (pusheado el 2026-08-31 vía `/supervisor-proyecto`, ver incidente en el log de sesiones) y que `/horarios` y `/dashboard` vuelven a mostrar datos (desde el parche del 2026-09-01, `/horarios` muestra el selector de grupos, no la lista de clases).
- Revocar/regenerar el Personal Access Token de Supabase (`sbp_...`) — se usó uno nuevo en Groundwork 3 (2026-08-31), otro nuevo en el Parche de retoques varios (2026-08-31), otro nuevo en F2 MOD 1 (2026-08-31), otro nuevo en F2 MOD 2 (2026-09-02), y otro nuevo en F2 MOD 3 (2026-09-02), sigue sin revocar ninguno de los usados en sesiones previas. (En F2 MOD 4, 2026-09-03, no se usó ningún token nuevo: la sesión no tuvo credenciales de Supabase y la migración quedó sin aplicar contra la base real, ver más abajo.)
- Corregir a mano en la app la tarea con Inicio 01/09/2026 y Vence 31/08/2026 (detectada en el Parche de retoques varios, 2026-08-31) — el usuario prefirió corregirla él mismo desde la UI ahora que el formulario valida el orden de fechas, en vez de que se corrija por SQL.
- ~~F2 MOD 1 (Planificaciones) sin verificar en el navegador~~ — **cerrado el 2026-08-31**: el usuario probó la UI después del parche de unificación y confirmó que está en orden, incluido el caso Jungla (eligiendo sábado, el bloque horario queda determinado por el día de la semana, que era justamente lo que antes obligaba al paso extra de elegir bloque a mano).
- No se probó en el navegador el parche del 2026-09-01 (sin herramienta de browser en esta sesión) — recomendado un vistazo manual: entrar a Planificaciones y confirmar que abre en el selector de grupo, que los filtros Todas/Activo/Cancelado funcionan dentro de un grupo y mes (y que el mes se conserva al cambiar de pestaña), que el contador de la campana muestra el número correcto y baja al abrir una notificación, y que "Marcar todas como leídas" lo deja en cero.
- Import del listado real de ~150 alumnas — sesión aparte, cuando Luciana entregue el archivo.
- **F2 MOD 4 (Asistencia): la migración `20260903120000_f2_mod4_asistencia.sql` todavía NO se aplicó contra el proyecto real de Supabase** — la sesión no tuvo ni el Personal Access Token ni las variables de entorno del proyecto, así que la validación se hizo contra un Postgres 16 local levantado en la sesión, aplicando toda la cadena de migraciones desde cero (saltando solo las dos que necesitan `pg_cron`/`pg_net`, que no están disponibles fuera de Supabase y no tocan Asistencia). Primer paso de la próxima sesión: aplicarla en Supabase.
- F2 MOD 4 (Asistencia): sin verificar en el navegador (la sesión no tuvo credenciales de la app ni base real contra la cual levantarla). Recomendado el repaso manual una vez aplicada la migración: cargar una fecha completa y confirmar que las no tildadas quedan ausentes; confirmar que el sábado nunca aparece como fecha disponible para Jungla; editar una fecha pasada y confirmar que se guarda; simular 3 semanas sin presente para una alumna de prueba y confirmar que aparece en "Alertas de inasistencia"; confirmar que un feriado en el medio no rompe ni suma la racha; y probar con la cuenta de Carolina (Profesor) que no ve "Asistencia" en el menú ni entra por URL directa a `/asistencia`, `/asistencia/alertas` ni `/asistencia/grupos/...`. La lógica de esos 5 primeros puntos sí quedó probada de forma automatizada en la sesión (ver el log), y los constraints y el RLS se probaron con SQL real contra el Postgres local.
- ~~F2 MOD 3 (Pagos): no se probó con una cuenta Profesor/Empleado real~~ — **cerrado el 2026-09-02**: el usuario pasó las credenciales de Carolina (Profesor) y Estefania (Empleado) y se confirmó con Playwright que ninguna de las dos ve "Administración"/"Pagos" en el menú, y que las 5 rutas de Pagos (`/pagos`, `/pagos/nuevo`, `/pagos/pendientes`, `/pagos/recaudacion`, `/pagos/deudoras`) redirigen a `/dashboard` al pegarlas directo.
- F2 MOD 3 (Pagos): no se simuló en vivo el banner de recordatorio de Deudoras (depende de que hoy sea día 8 o 9, no se forzó el reloj del sistema) — misma lógica de fecha ya probada para el recargo.
- Decisión pendiente sin resolver (mencionada en el punto 2): el rol `'Secretaria'` para Dai sigue sin nombre ni alcance definido. La RLS de `alumnas`/`contactos` (Fase 2, Sesión 1), la de `pagos`/`pagos_metodos` (F2 MOD 3) y la de `asistencia` (F2 MOD 4) ya quedaron escritas contra ese rol de antemano, así que en cuanto se decida y se agregue `'Secretaria'` al CHECK de `users.rol`, no hace falta otra migración de permisos para esas tablas — solo reasignar el rol de Dai.
- Mapeo manual de los 4 turnos existentes con `grupo_legacy` (texto libre viejo) a un `grupo_id` real (Fase 1.2, Sesión 2, 2026-08-31) — Lauti los abre uno por uno en "Editar clase" y elige el grupo correcto (el formulario muestra el texto viejo como ayuda). Recién cuando estén los 4 mapeados se puede dropear `grupo_legacy` en una migración aparte.
- No se probó en el navegador el formulario de Clases/Turnos con el select de grupo y la pregunta de bloque de Jungla (sin herramienta de browser en esta sesión) — recomendado un vistazo manual: crear/editar un turno con un grupo de un solo bloque y con Jungla (dos bloques), y confirmar que el horario mostrado es el correcto en ambos casos.
- No se probó en el navegador el checklist de varios profesores por clase ni el color ámbar de "Cancelar clase" (Groundwork 3, 2026-08-31, sin herramienta de browser en esta sesión) — recomendado un vistazo manual: crear/editar una clase asignando dos profesores (Admin o Head Coach), confirmar que ambos la ven en el detalle y que a ambos les llega la notificación de asignación (campana + push); confirmar visualmente que "Cancelar clase" se ve ámbar/distinto de "Borrar clase" (rojo).

**Confirmado por el usuario después del audit (2026-08-27):** probó el flujo real (no la llamada directa a la Edge Function) asignándole una tarea a Luciana desde su propia cuenta, y el push le llegó a ella en su dispositivo. Con esto quedan cerrados los dos huecos de verificación que el audit de `/supervisor-proyecto` había dejado abiertos (push a la cuenta de Luciana, y flujo real vía asignación en vez de invocación directa) — el único pendiente real es la revocación del token.

## 8. Log de sesiones

<!-- Cada sesión agrega una línea acá al terminar -->

| Fecha | Módulo trabajado | Resultado |
|---|---|---|
| 2026-08-17 | 1 — Setup base | Next.js + TypeScript + Tailwind v4 configurados, build local OK. Git inicializado, primer commit pusheado a `github.com/lautaroloza89-glitch/PSG---System` (main). Proyecto Supabase creado y keys cargadas en `.env.local`. Deploy vacío funcionando en `https://psgsystem.vercel.app` (200 OK, placeholder visible). Módulo cerrado ✅. |
| 2026-08-17 | 2 — Modelo de datos | Definidas 5 tablas (`users`, `tareas`, `tarea_asignados`, `tarea_comentarios`, `turnos`) según decisiones acordadas: tareas y proyectos unificados en una sola tabla, tareas con varios responsables (many-to-many), comentarios como historial, turnos por fecha puntual (no recurrente) con estado Activo/Cancelado para poder cancelar un día sin borrar el registro. Migración `supabase/migrations/20260817120000_modelo_datos_inicial.sql` aplicada contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API, verificada (5 tablas + RLS habilitada en todas, sin políticas aún — se definen en el Módulo 3). `docs/modelo-datos.md` actualizado con el modelo final. Tipos TypeScript creados en `src/types/` (`User`, `Tarea`, `TareaAsignado`, `TareaComentario`, `Turno`). `tsc --noEmit` sin errores. Módulo cerrado ✅. |
| 2026-08-17 | 3 — Auth + roles | Decisiones de producto acordadas: sin auto-registro público, el Admin da de alta la cuenta desde Supabase Auth (Dashboard) y el login es email+password; un trigger (`handle_new_user`) crea automáticamente la fila en `public.users` leyendo `nombre`/`rol` de los metadatos del usuario (default `rol='Empleado'` si no vienen, mínimo privilegio). Instalado `@supabase/ssr`; creados los clientes de Supabase (`src/lib/supabase/client.ts`, `server.ts`, `middleware.ts`) y el helper `getCurrentUserProfile()`. `middleware.ts` en la raíz protege las rutas de `(dashboard)` (redirect a `/login` sin sesión, redirect a `/dashboard` si ya hay sesión estando en `/login`). Página de login (`src/app/(auth)/login`, email+password, sin registro) y layout protegido de `(dashboard)` con nombre/rol del usuario y botón de cerrar sesión. Políticas RLS completas para las 5 tablas aplicadas en migración `supabase/migrations/20260817140000_auth_roles_rls.sql` (Admin todo; Profesor sus tareas creadas/asignadas y sus turnos; Empleado solo lo asignado, cambia estado + comenta, bloqueado a nivel trigger para el resto de las columnas; `users` y `turnos` de lectura abierta a cualquier autenticado). Se encontró y corrigió una recursión infinita de RLS en las políticas de `tarea_asignados` (se resolvió con la función `security definer` `usuario_asignado_a_tarea`). Probado de punta a punta con 3 cuentas reales (2 Admin, 1 de prueba alternada entre Empleado/Profesor): 21/21 checks OK (login, visibilidad por rol, edición restringida, trigger anti-escalado del Empleado, aislamiento entre Profesores). `tsc --noEmit` y `next build` sin errores. Módulo cerrado ✅. |
| 2026-08-18 | 4 — Módulo Tareas | Decisiones de UI acordadas: responsables se eligen con un checklist de personas (equipo chico, cómodo en mobile); listado con filtro simple por estado (Todas/Pendiente/En progreso/Completada, sin buscador); vista en tarjetas (mejor para PWA en celular). Construido en `src/app/(dashboard)/tareas/`: listado (`page.tsx`, con filtro por `?estado=`), alta (`nueva/page.tsx`), detalle con cambio de estado y comentarios (`[id]/page.tsx`), edición (`[id]/editar/page.tsx`), y server actions en `actions.ts` (`crearTarea`, `editarTarea`, `actualizarEstadoTarea`, `agregarComentario`) que se apoyan en la RLS del Módulo 3 sin duplicarla en el frontend (solo se replica en las páginas el mismo criterio de "puede editar" para mostrar u ocultar el link de edición). Componentes reutilizables en `src/components/tareas/` (`TareaCard`, `EstadoBadge`, `FiltroEstadoTabs`, `AsignadosChecklist`, `TareaForm`, `EstadoSelector`, `ComentariosList`, `ComentarioForm`). Probado de punta a punta con Playwright contra las 3 cuentas reales (2 Admin, 1 de prueba alternada Empleado/Profesor, devuelta a Empleado al cerrar): alta/edición/cambio de estado/comentarios como Admin y Profesor, aislamiento de visibilidad entre Profesores (solo ve lo propio o asignado, 404 en lo ajeno), Empleado sin botón ni ruta de creación/edición (redirige), cambio de estado y comentario permitidos para Empleado, filtro por estado funcionando. Datos de prueba borrados al terminar. `tsc --noEmit` y `next build` sin errores; se observó un warning de hidratación en consola (`caret-color: transparent` en el textarea de comentarios) solo bajo Chromium headless automatizado, no reproducido en la app real ni afecta la funcionalidad — a confirmar en un uso manual normal. Módulo cerrado ✅. |
| 2026-08-18 | 5 — Módulo Horarios/Turnos | Decisión de producto acordada: el dropdown de "profesor" en el formulario de turnos debe mostrar tanto a los usuarios con `rol = 'Profesor'` como a los `Admin` que también dictan clases (la Head Coach/dueña sí dicta, la Secretaria no, y ambas comparten el rol de permisos `Admin`). Para eso se agregó a `users` una columna nueva `dicta_clases boolean not null default false` (migración `supabase/migrations/20260818150000_users_dicta_clases.sql`, aplicada contra el proyecto real vía Management API y registrada en `supabase_migrations.schema_migrations`) — no es un rol de permisos nuevo, no toca RLS de turnos. Con el usuario se confirmó que `patinsaintgermain@gmail.com` es la Head Coach/dueña (`dicta_clases = true`); `lautaroloza89@gmail.com` queda en `false` (Secretaria aún no cargada como usuaria). Construido en `src/app/(dashboard)/horarios/`: listado con filtro por estado (`page.tsx`, `?estado=`), alta (`nuevo/page.tsx`), detalle (`[id]/page.tsx`), edición (`[id]/editar/page.tsx`), y server actions en `actions.ts` (`crearTurno`, `editarTurno`, `actualizarEstadoTurno`, `borrarTurno`) siguiendo el mismo patrón que Tareas: se apoyan en la RLS del Módulo 3 (Admin todo; Profesor solo sus turnos, no puede reasignar `profesor_id`; Empleado solo lectura) y replican en las páginas el mismo criterio de "puede editar" para mostrar u ocultar acciones. La cancelación es un toggle Activo/Cancelado (agrega también "reactivar", no solo cancelar, ya que usa la misma acción y el mismo criterio de permisos que editar) en vez de un delete; el borrado real queda aparte y visible solo para Admin, con confirmación. Componentes en `src/components/horarios/` (`TurnoCard`, `EstadoTurnoBadge`, `FiltroEstadoTurnoTabs`, `TurnoForm`, `ToggleEstadoTurnoButton`, `BorrarTurnoButton`). `docs/modelo-datos.md` actualizado con la columna nueva. Probado de punta a punta con Playwright contra las 3 cuentas reales (2 Admin, 1 de prueba alternada Profesor/Empleado, devuelta a Empleado al cerrar): 35/35 checks OK — Admin (15: alta con dropdown de profesor incluyendo a la dueña, edición, cancelar/reactivar, borrado real, filtros), Profesor (13: autoasignación sin dropdown en alta y edición, cancelar/reactivar su turno, sin botón de borrar, bloqueado para editar turno ajeno tanto por UI como por redirect directo a la URL), Empleado (7: ve el horario compartido de solo lectura, sin botón de alta, redirigido si entra directo a `/horarios/nuevo` o a una edición, sin botones de cancelar/borrar). Datos de prueba borrados al terminar. Efecto colateral detectado y corregido en la sesión: un `next build` corrido para verificar el módulo pisó la carpeta `.next` de un dev server que ya estaba corriendo en el puerto 3000 (PID 3752, no iniciado por esta sesión) y lo dejó sirviendo chunks viejos (`ERR_ABORTED` / 404 en assets); no se pudo reiniciar ese proceso por una restricción del entorno de la sesión, así que las pruebas se corrieron contra un dev server nuevo en el puerto 3001 (cerrado al terminar) — **si el puerto 3000 sigue sirviendo assets rotos, reiniciar manualmente `npm run dev`**. `tsc --noEmit` y `next build` sin errores. Módulo cerrado ✅. |
| 2026-08-18 | 6 — Dashboard | Decisiones de producto acordadas con el usuario antes de construir: (1) los turnos del dashboard muestran el horario completo compartido para los 3 roles (no solo los turnos propios del Profesor), ya que la RLS de Turnos ya es de lectura abierta a cualquier autenticado; (2) el Admin ve, además de las listas, una fila de 4 contadores agregados (tareas pendientes, tareas en progreso, turnos de hoy, turnos de los próximos 7 días) — Profesor y Empleado no ven contadores, solo las listas; (3) el criterio de "próximos" es un tope fijo de 5 ítems (no una ventana de días), ordenados por fecha ascendente, para que las tareas vencidas/atrasadas queden siempre arriba. Completado `src/app/(dashboard)/dashboard/page.tsx` (antes placeholder del Módulo 3): dos queries de solo lectura (tareas no completadas ordenadas por `fecha_vencimiento`, turnos activos con `fecha >= hoy` ordenados por fecha/hora, ambas con `.limit(5)`) que se apoyan en la RLS del Módulo 3 sin duplicar lógica de permisos — el alcance por rol lo resuelve la propia RLS (Admin ve todas las tareas, Profesor/Empleado solo las suyas; turnos siempre completos). Reutiliza los componentes ya existentes `TareaCard` y `TurnoCard` de los Módulos 4 y 5 en vez de crear una vista propia. Nuevo componente `src/components/dashboard/StatCard.tsx` para los contadores del Admin (los 4 counts se calculan con 4 queries `count: "exact", head: true` en paralelo, solo cuando `profile.rol === "Admin"`). Probado de punta a punta con Playwright contra las 3 cuentas reales (2 Admin, 1 Empleado — `lautaroloza88@gmail.com` estaba en rol Empleado al momento de la prueba; no se testeó Profesor por separado porque la política RLS de `select` de tareas no distingue entre Profesor y Empleado, así que el mismo chequeo con Empleado ya cubre esa rama de código, y el único branch propio de este módulo, `rol === "Admin"`, sí se probó con las 2 cuentas Admin) con datos de prueba temporales (tareas y turnos con prefijo `TEST-DASH`, creados y borrados vía script con `@supabase/supabase-js`, confirmados en 0 al final): contadores de Admin exactos (1 pendiente, 1 en progreso, 1 turno hoy, 2 turnos en la semana), orden correcto (tarea vencida primero), tarea "Completada" correctamente excluida de la lista, Empleado sin contadores y con sus tareas asignadas + horario compartido completo, links "Ver todas"/"Ver todos" navegando a `/tareas` y `/horarios`. `tsc --noEmit` y `next build` sin errores (build corrido sin dev server activo en el puerto por defecto, evitando el problema de la sesión anterior). Módulo cerrado ✅. |
| 2026-08-19 | Fix retroactivo — `turnos.capacidad` pasa a opcional | Línea agregada retroactivamente el 2026-08-29 (auditoría `/auditoria-psg`, sesión 2/3 de Fase B — `docs/auditoria-fase-b-2-db-aditivo.md`, hallazgo #1): esta migración ya estaba aplicada en producción (`supabase/migrations/20260819120000_turnos_capacidad_opcional.sql`, `turnos.capacidad` confirmado `nullable = YES` contra la base real) pero nunca había quedado registrada en este log. Corrección mobile: se sacó el campo "Capacidad" del formulario de turnos; la columna se mantiene en la tabla (no se borra), pero deja de ser `not null` para no forzar un valor inventado en los turnos nuevos. |
| 2026-08-22 | Extensión al Módulo 3 — roles Head Coach/Patinador + cargo | Pedido documentado en `docs/roles-actualizacion.md` (nuevo). Decisiones acordadas: nombres exactos de rol `'Head Coach'` (con espacio) y `'Patinador'`; ninguna cuenta real se migra a estos roles en esta sesión (el Admin los asigna después a mano); se actualizó también la tabla "Roles del sistema" (punto 2 de este documento), no solo este Log; el filtro de "profesor asignable a un turno ajeno" (`rol === "Profesor" \|\| dicta_clases`) queda sin tocar, fuera de alcance. Migración `supabase/migrations/20260822120000_roles_head_coach_patinador.sql` aplicada contra el proyecto real vía Management API y registrada en `supabase_migrations.schema_migrations`: CHECK de `users.rol` ampliado a 5 valores, columna `users.cargo` (text, opcional, sin efecto en permisos) agregada, función `tarea_visible_para_head_coach()` nueva, y políticas RLS actualizadas — Head Coach entra en las mismas ramas que ya tenía Profesor para lo propio (`tareas_insert`, `tareas_delete`, `tarea_asignados_insert/delete`, `turnos_insert`, `turnos_update`) más una rama nueva de lectura en `tareas_select`/`tarea_asignados_select`/`tarea_comentarios_select` (tareas creadas por o asignadas a usuarios con rol Profesor/Empleado/Patinador); Patinador se sumó a `tareas_update_empleado` (renombrada `tareas_update_empleado_patinador`) y al trigger anti-escalado `check_tarea_update_empleado`. `turnos_select` ya era de lectura abierta, sin cambios. Se sumaron también los gates de permiso a nivel aplicación en `tareas/actions.ts`, `horarios/actions.ts` y las páginas de ambos módulos (Patinador donde se excluía a Empleado, Head Coach donde se incluía a Profesor). Nuevo componente compartido `src/components/ui/UsuarioRolCargo.tsx` (nombre opcional, rol en negrita, cargo debajo si existe, sin renderizar nada si `cargo` es `null`), usado en `AsignadosChecklist`, `MiembroCard` y en el detalle/tarjetas de tarea (`TareaCard`, `tareas/[id]/page.tsx`, `tareas/page.tsx`, `dashboard/page.tsx`) — estos últimos antes solo mostraban el nombre del responsable, ahora también rol y cargo. Verificado: `tsc --noEmit` y `next build` sin errores (se detuvo el `next dev` que estaba corriendo en el puerto 3000 para el build, y se volvió a levantar al terminar). RLS verificada por simulación SQL contra el proyecto real dentro de transacciones con `rollback` (sin persistir cambios en las 3 cuentas reales): Head Coach puede crear/editar/borrar su propia tarea y turno y editar sus responsables; Head Coach ve una tarea creada por un Profesor ajeno pero NO puede editarla; Patinador no puede crear tareas/turnos; el trigger anti-escalado bloquea a Patinador cambiar título pero permite cambiar estado; Patinador no puede tocar una tarea que no tiene asignada. La cuenta de prueba (`lautaroloza88@gmail.com`) quedó devuelta a su rol original `Empleado` sin `cargo`. No se verificó visualmente la UI de `UsuarioRolCargo` en el navegador (sin herramienta de browser disponible en esta sesión) — recomendado un vistazo manual rápido a `/miembros` y al formulario de nueva tarea. Módulo cerrado ✅. |
| 2026-08-25 | Ajuste de permisos — Head Coach con control total sobre Turnos | Reportado por el usuario probando con la cuenta real de Luciana (Head Coach): al editar un turno no aparecía el dropdown de "Profesor", solo el texto de autoasignación — comportamiento esperado bajo las reglas vigentes desde el 2026-08-22 (Head Coach = mismos permisos que Profesor sobre lo propio), pero no lo que el negocio necesita: Luciana es quien arma la mayor parte de las planificaciones de clases y necesita poder asignárselas a cualquier profesor. Confirmado con el usuario el alcance exacto (pregunta de alto impacto sobre RLS): Head Coach pasa a tener sobre **turnos** el mismo control total que Admin (crear y reasignar cualquiera); sobre **tareas** el alcance no cambió. El borrado de turnos sigue Admin-only, no se pidió para Head Coach. Migración `supabase/migrations/20260825160000_head_coach_asigna_turnos.sql` aplicada contra el proyecto real vía Management API y registrada en `supabase_migrations.schema_migrations`: `turnos_insert`/`turnos_update` pasan de `Admin or (Profesor\|Head Coach and profesor_id=self)` a `(Admin\|Head Coach) or (Profesor and profesor_id=self)`, verificado leyendo `pg_policies` después de aplicar. Cambios de aplicación en `src/app/(dashboard)/horarios/`: `TurnoForm.tsx` muestra el dropdown de profesor también a Head Coach; `actions.ts` (`crearTurno`/`editarTurno`) deja de forzar `profesor_id = self` para Head Coach (sigue forzado solo para Profesor); `[id]/page.tsx` y `[id]/editar/page.tsx` mueven a Head Coach a la misma rama de `puedeEditar` que Admin (antes compartía rama con Profesor, restringida a lo propio). Detalle completo en `docs/roles-actualizacion.md` (sección "Ampliación 2026-08-25"). `tsc --noEmit` sin errores; `next build` no se pudo confirmar en esta sesión por un `ENOENT` en `.next` aparentemente causado por otra sesión de Claude Code compilando el mismo proyecto en paralelo (evidencia: `AppHeader.tsx` cambió en disco durante la sesión con un `NotificacionesBell` del Módulo 7, que esta sesión no tocó) — **recomendado correr `next build` una vez sola esa sesión paralela termine, para confirmar que el build de producción sigue limpio**. No se probó E2E con la cuenta real de Head Coach (sin herramienta de browser en esta sesión) — recomendado un vistazo manual: reasignar un turno ajeno logueado como Luciana. |
| 2026-08-25 | Fix — usuario actual en el menú hamburguesa | Fix puntual, no es un módulo nuevo (tabla del punto 5 sin cambios). El menú global (`src/components/ui/AppHeader.tsx`) no mostraba qué cuenta estaba logueada. Se agregó, arriba de todo dentro del panel desplegable y antes de la lista de secciones, el bloque nombre/rol/cargo del usuario actual (separado con una línea antes de la nav), reusando el componente ya existente `UsuarioRolCargo` (el mismo que ya se usa en `AsignadosChecklist`/`MiembroCard`, ver `docs/roles-actualizacion.md`) en vez de crear uno nuevo. El perfil ya se cargaba en `src/app/(dashboard)/layout.tsx` (`getCurrentUserProfile()`); se pasó como prop nueva `profile` a `AppHeader` (antes no recibía props). `tsc --noEmit` y `next build` sin errores (sin dev server activo en ese momento, no hizo falta el workaround del puerto). No se verificó visualmente en el navegador (sin herramienta de browser disponible en esta sesión) — recomendado un vistazo manual rápido al menú con alguna de las cuentas reales. |
| 2026-08-25 | 7 (sesión 1/4) — Notificaciones: tarea asignada | Alcance de esta sesión acordado con el usuario: solo el disparador de "tarea asignada" en tiempo real (comentarios, vencimientos y push quedan para las sesiones 2-4). Tabla `notificaciones` (`usuario_id`, `tipo` texto libre, `mensaje`, `tarea_id`, `leida`, `creado_en`) y trigger `notificar_tarea_asignada` sobre `tarea_asignados` — no sobre `tareas`, porque los responsables son muchos a muchos (migración `supabase/migrations/20260825150000_notificaciones.sql`, aplicada vía Management API y registrada en `supabase_migrations.schema_migrations`). El trigger no notifica autoasignación (`auth.uid() = usuario_id`) y solo dispara por cada fila nueva en `tarea_asignados`, no reenvía a quien ya estaba asignado. Para que eso funcione en una reasignación, se corrigió `editarTarea` (Módulo 4, ya cerrado — cambio explícitamente autorizado por el usuario tras consultarlo): antes borraba y reinsertaba todos los responsables en cada edición; ahora calcula el diff (altas/bajas) y solo toca las filas que cambiaron. RLS de `notificaciones`: cada usuario ve y marca como leídas solo las propias; no hay política de `insert` para la app (la crea el trigger, `security definer`, mismo patrón que `handle_new_user`). Tabla sumada a la publicación `supabase_realtime`. UI: campana en `AppHeader` junto al ☰ (`src/components/notificaciones/NotificacionesBell.tsx`) con punto de "sin leer", lista simple (mensaje + tiempo relativo), suscripción Realtime filtrada por `usuario_id`, marca como leída y navega a la tarea al tocar una notificación. Probado E2E con Playwright contra 3 cuentas reales (Lautaro-Admin creador, lautaroloza88-Empleado asignado inicial, Luciana-Head Coach reasignada): 12/12 checks OK — autoasignación sin notificación propia, notificación en vivo sin recargar, mensaje correcto, click navega y marca leída, reasignación notifica solo a la persona nueva y no reenvía a la anterior. Datos de prueba borrados (0 restantes, confirmado). `tsc --noEmit` y `next build` sin errores (sin dev server activo). Se detectó y resolvió en la sesión un choque con otra sesión trabajando en paralelo sobre el Módulo 5 (permisos de Head Coach en turnos, ver log de abajo): se commitearon por separado (`e89df3f` solo con los 9 archivos propios de este módulo, encima de `d000108` de la otra sesión), sin mezcla ni pérdida de cambios; build combinado final limpio. Verificado en una revisión posterior (`/supervisor-proyecto`): contenido del commit coincide con lo declarado, trigger/tabla/políticas/publicación realtime confirmados vivos en la base real, y 3 notificaciones reales generadas por el equipo después del cierre de la sesión (`dai@gmail.com`, `keyla@gmail.com`) confirman que el disparador ya funciona en producción. Sesión cerrada 🟡 (no ✅: quedan 3 sesiones más del mismo módulo). |
| 2026-08-25 | Carga de roles y cargos reales del equipo | El usuario probó el rol Head Coach en la app real con la cuenta de prueba (`lautaroloza88@gmail.com` temporalmente en `Head Coach`, cargo "Preparadora Física (prueba)") y confirmó que la lectura ampliada y los permisos sobre lo propio funcionan como se esperaba; devuelta a `Empleado` sin `cargo` al terminar. Se detectó un problema de encoding al actualizar `cargo` por la Management API pasando el texto inline por bash (la tilde de "Física" se corrompía a `U+FFFD`) — se resolvió escribiendo el JSON a un archivo y enviándolo con `--data-binary`, que preserva el UTF-8 correctamente; usar ese método para cualquier valor con tildes/ñ en sesiones futuras. Con el usuario se decidió el rol y cargo real de cada persona del equipo (aplicado directo en `public.users` vía Management API, las cuentas de Auth las creó el usuario a mano desde el Dashboard de Supabase porque esta sesión no tiene acceso a la `service_role key`): Luciana Giacometti pasó de `Admin` a `Head Coach` (cargo "Entrenadora Principal") — decisión confirmada explícitamente por el usuario sabiendo que implica perder los permisos de Admin (gestión de usuarios, ver/editar todo); Lautaro (`lautaroloza89@gmail.com`) sigue siendo `Admin` y queda como el único Admin real del sistema; Dai (Secretaria) se creó con rol `Admin` (cargo "Secretaria"), anticipando el control que va a necesitar para el futuro módulo de cuotas/pagos, aunque hoy le da más permisos de los que usa; Keyla quedó en `Profesor` (cargo "Preparadora Física", para poder crear/gestionar sus propias tareas y horarios — "cargar planificaciones"); Carolina, Estefania y Malena quedaron en `Empleado` (cargos "Entrenadora Asistente", "Ayudante" y "Ayudante" respectivamente). Tabla "Mapeo de puestos → rol" del punto 2 reemplazada por el mapeo real con nombres, emails, roles y cargos. **Decisión de producto pendiente, no implementada:** más adelante se evalúa crear un rol nuevo para Dai, más acotado que Admin — permisos amplios sobre el dominio administrativo (cuando exista ese módulo) sin el control total que hoy le da Admin sobre Tareas/Horarios/gestión de usuarios; sin nombre ni alcance definido todavía, no asumir nada al respecto sin volver a preguntar. |
| 2026-08-26 | 7 (sesión 2/4) — Notificaciones: comentario nuevo | Reutilizada la misma tabla `notificaciones` y la misma campana (`NotificacionesBell`) de la sesión 1, sin crear tabla ni componente nuevos. Antes de escribir el trigger se preguntó al usuario (decisión de alto impacto, sin criterio previo): a quién avisa un comentario nuevo y el mensaje exacto; el usuario delegó la decisión ("recomendame vos"), así que se tomó y se dejó documentada: destinatarios = unión de responsables asignados (`tarea_asignados`) + creador de la tarea (`tareas.created_by`), sin duplicados, menos el autor del comentario (así el creador se entera aunque haya delegado la tarea sin quedar asignado); mensaje = `"{Nombre} comentó en: {título}"` (sin el texto del comentario, para no filtrar contenido en una notificación corta). Migración `supabase/migrations/20260826120000_notificaciones_comentario_nuevo.sql` con el trigger `notificar_comentario_nuevo` (security definer, dispara en cada `insert` real sobre `tarea_comentarios`, tipo `'comentario_nuevo'`), aplicada vía Management API y registrada en `supabase_migrations.schema_migrations`. No se tocó el trigger de "tarea asignada" ni la UI de la campana: `NotificacionesBell` ya renderiza `mensaje` y navega por `tarea_id` de forma genérica sin importar el `tipo`, así que no hizo falta ningún ajuste. Probado con una simulación SQL directa contra el proyecto real dentro de una transacción con `rollback` (sin tocar datos reales, mismo método que la extensión de roles del 2026-08-22): tarea de prueba con creador (Lautaro, no asignado) y dos asignados (Keyla, lautaroloza88); comentario de Keyla generó notificación para lautaroloza88 (asignado) y para Lautaro (creador no asignado), y ninguna para Keyla (autora) — 0 filas de prueba remanentes tras el rollback, confirmado. `docs/modelo-datos.md` actualizado con el trigger nuevo. `tsc --noEmit` y `next build` sin errores (sin dev server activo). No se probó en el navegador con Realtime en vivo (sin herramienta de browser en esta sesión) — recomendado un vistazo manual: comentar una tarea ajena y confirmar que la campana del destinatario se actualiza sin recargar. Sesión cerrada 🟡 (quedan 2 sesiones más del mismo módulo). |
| 2026-08-26 | Verificación post-sesión 2/4 — falsa alarma de Realtime | Vía `/supervisor-proyecto`: se re-verificó de forma independiente lo declarado en la sesión anterior contra la base real (trigger, función y migración presentes; políticas RLS de `tarea_comentarios` y `notificaciones` sin cambios; rama al día con `origin/main`, sin choques de otra sesión); `tsc --noEmit` reejecutado limpio. El usuario reportó después, probando en producción, que la notificación de comentario no le llegaba en vivo a la cuenta de Luciana (solo al refrescar la página), aunque sí le llegaba en vivo a la cuenta de Admin. Se investigó antes de tocar código: se descartó que fuera el trigger (ya verificado simétrico para ambos usuarios) y se le pidió al usuario una prueba de descarte — invertir qué cuenta usa la ventana de incógnito y cuál la ventana normal del navegador. Resultado: con Luciana en incógnito y Admin en ventana normal, todo funcionó en vivo para ambas. Esto confirma que **no era un bug de la app**: el problema era algo local de esa ventana/perfil "normal" específico (extensión, antivirus, o una pestaña vieja con un canal Realtime duplicado) — no del código, no de RLS, no del trigger. Sin cambios de código. La duda pendiente de la sesión 2 ("no se probó Realtime en vivo") queda resuelta: el mecanismo funciona correctamente. |
| 2026-08-26 | 7 (sesión 3/4) — Notificaciones: tareas por vencer/vencidas | A diferencia de las sesiones 1 y 2 (disparadas por un `insert` obvio), acá no hay ningún evento de base de datos que marque "se acerca el vencimiento" — se preguntó al usuario antes de escribir nada (mecanismo, umbral, repetición, alcance de vencidas) y se resolvió: mecanismo `pg_cron` (recomendado por Claude sobre un chequeo al cargar el dashboard, para que el aviso llegue aunque nadie entre a la app ese día), extensión habilitada y job `notificar_tareas_vencimiento_diario` corriendo `0 11 * * *` (08:00 Argentina) llamando a la función nueva `public.notificar_tareas_vencimiento()` (security definer). Umbral en días completos, no en horas, porque `fecha_vencimiento` es `date` sin componente de hora (la propuesta inicial del usuario de "48hs/16hs antes" no se podía calcular con precisión con ese tipo de dato, se lo señalé y se acordó "2 días antes" / "1 día antes" en su lugar); se incluyó también el aviso de "vencida" en esta misma sesión (ya estaba en el alcance original del punto 5). Destinatarios: mismo criterio que la sesión 2 (asignados + creador, sin duplicados, sin excluir a nadie porque no hay "autor de la acción"). Cada aviso ('tarea_por_vencer_2d', 'tarea_por_vencer_1d', 'tarea_vencida') se genera una sola vez por tarea/destinatario, chequeando antes de insertar que no exista ya esa combinación — así el cron diario no reenvía el mismo aviso mientras la tarea sigue pendiente. Reutilizada la tabla `notificaciones` y la campana (`NotificacionesBell`) sin tocarlas, igual que la sesión 2. Migración `supabase/migrations/20260826130000_notificaciones_vencimiento.sql` aplicada vía Management API y registrada en `supabase_migrations.schema_migrations`; verificado el job vivo en `cron.job` (`jobid=1`, `active=true`) y la función con `security definer=true`. Probado con datos de prueba reales (prefijo `TEST-VENC`, 5 escenarios: 2 días, 1 día con creador=asignado, vencida, completada-no-avisa, muy-lejos-no-avisa) — los 5 se comportaron como se esperaba, incluyendo que el creador=asignado no recibe el aviso duplicado; se confirmó además que correr la función dos veces seguidas no duplica notificaciones (dedup funcionando). Datos de prueba borrados (0 tareas y 0 notificaciones restantes, confirmado). `docs/modelo-datos.md` actualizado con el detalle completo de la sesión. `tsc --noEmit` y `next build` sin errores (sin dev server activo). No se probó el disparo real del cron en producción (recién corre por primera vez a las 08:00 Argentina) ni se vio la campana en el navegador con un aviso real de vencimiento (sin herramienta de browser en esta sesión) — recomendado revisar mañana que la campana haya recibido algún aviso real si hay tareas próximas a vencer. Sesión cerrada 🟡 (queda 1 sesión más del mismo módulo — push). |
| 2026-08-26 | Aclaración post-sesión 3/4 — el aviso real no vino del cron | El usuario reportó que le llegó una notificación de vencimiento a los pocos minutos del commit de la sesión 3. Investigado antes de asumir que el cron ya había disparado: `cron.job_run_details` tiene **0 corridas totales** para el job — no disparó ni una sola vez todavía (el job se creó hoy después de las 11:00 UTC/08:00 Argentina, así que su primera corrida real es mañana a esa hora). El aviso real que llegó (`tarea_vencida` para "Fisico de las 16hs" y `tarea_por_vencer_2d` para "Coaching Deportivo", ambas tareas reales del equipo, timestamp `2026-08-26 22:04:27 UTC`) fue un efecto secundario de las pruebas de la sesión 3: la función `notificar_tareas_vencimiento()` no distingue datos de prueba de datos reales, evalúa toda la tabla `tareas` — al invocarla manualmente para probar los 5 escenarios sintéticos (`TEST-VENC`), también barrió (correctamente) las tareas reales que ya cumplían el criterio en ese momento y les generó el aviso de verdad, antes de lo previsto. El contenido no es incorrecto ("Coaching Deportivo" efectivamente vence el 28/08, a 2 días; "Fisico de las 16hs" estaba vencida en ese momento y luego se completó, probablemente en respuesta al aviso) — el sistema funcionó, solo que el disparo fue manual y no el del cron. Gracias al dedup por `(tarea_id, usuario_id, tipo)`, el cron real de mañana no va a reenviar estos mismos avisos. **Lección para sesiones futuras:** al probar una función tipo batch/cron que procesa toda la tabla (a diferencia de un trigger acotado a la fila insertada), conviene envolver la prueba en una transacción con `rollback` en vez de llamarla directo contra producción, para no generar notificaciones reales a usuarios reales como efecto secundario de una prueba. **Sigue pendiente de verificar:** que `cron.job_run_details` muestre al menos una corrida real después de las 08:00 Argentina de mañana. |
| 2026-08-27 | Verificación de la primera corrida real del cron (cierra la duda de la sesión 3/4) | Se había programado una rutina cloud automática para revisar esto apenas pasaran las 08:00 Argentina, pero falló: el entorno sandbox donde corren las rutinas cloud bloquea por política de red el acceso saliente a `api.supabase.com` (403 en el proxy de egress), así que no pudo ejecutar ninguna query — reportó el fallo por push en vez de asumir un resultado. Se repitió la verificación manualmente desde esta sesión (con acceso a internet normal): `cron.job_run_details` muestra la corrida real de hoy a las `11:00:00 UTC` con `status='succeeded'` (`jobid=1`, `return_message='1 row'`); se generaron 2 notificaciones reales (`tipo='tarea_por_vencer_1d'`, "Vence mañana: Coaching Deportivo") para los dos destinatarios correspondientes, con `creado_en` igual al timestamp exacto de la corrida del cron — sin duplicados. El mecanismo (Módulo 7, sesión 3/4) queda confirmado funcionando en producción sin intervención manual. **Lección:** las rutinas cloud (`/schedule`) no tienen salida de red abierta a servicios externos arbitrarios (Management API de Supabase incluida); para verificaciones que dependan de un servicio externo específico, contemplar que puedan fallar por política de red y no asumir que el fallo silencioso significa que el chequeo no hacía falta. |
| 2026-08-27 | 7 (sesión 4/4) — Notificaciones push · Módulo 7 cerrado ✅ | Decisiones acordadas con el usuario antes de escribir código: proveedor **Web Push API nativo** (recomendado por Claude sobre OneSignal/Firebase, para no sumar una cuenta ni una dependencia externa a un equipo de 10 personas cuando todo lo demás vive en Supabase); permiso del navegador se pide **automáticamente al loguearse** (el usuario va a avisarle al equipo de antemano que acepten, así que no hacía falta un botón aparte); cubre **los 3 tipos** de aviso existentes (asignación, comentario, vencimiento), sin excepciones. Arquitectura: tabla nueva `push_subscriptions` (RLS propia, sin política de `update` — ver detalle y limitación conocida en `docs/modelo-datos.md`); extensión `pg_net` instalada (antes no estaba, `create extension` bloqueado una vez por el clasificador de auto mode por ser una acción sensible de infraestructura, reintentado con aprobación explícita del usuario); secreto compartido guardado en `supabase_vault` (extensión ya habilitada, sin uso hasta esta sesión); Edge Function nueva `send-push` (`supabase/functions/send-push/index.ts`, Deno + `web-push`, desplegada con `verify_jwt=false` y protegida por el secreto compartido vía header) con sus secrets (`VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `PUSH_SHARED_SECRET`) cargados vía Management API; trigger nuevo `notificaciones_push` (`after insert on notificaciones`) que **no toca los 3 triggers de las sesiones 1-3** — se engancha directo a la tabla `notificaciones`, así cubre los 3 tipos sin duplicar lógica. Migración `supabase/migrations/20260827120000_notificaciones_push.sql` aplicada vía Management API (en 3 bloques, límite de tamaño conocido) y registrada en `supabase_migrations.schema_migrations`. Frontend: `public/sw.js` (Service Worker nuevo — el proyecto no tenía ninguno pese a estar pensado como PWA desde el Módulo 1, se creó el mínimo necesario para recibir `push`/`notificationclick`, sin tocar el resto del setup PWA que sigue incompleto y fuera de alcance); `src/lib/push/subscribe.ts` y `actions.ts` (pide permiso, se suscribe, guarda en `push_subscriptions`); enganchado en `src/app/(auth)/login/page.tsx` sin bloquear la navegación al dashboard. `NEXT_PUBLIC_VAPID_PUBLIC_KEY` agregado a `.env.local` — **pendiente que el usuario lo cargue también en Vercel** (env var pública, valor: ver `.env.local`), no tengo acceso a esa parte. Verificado con una prueba real de infraestructura (no de UI): insertada una fila de prueba en `notificaciones` para la cuenta Admin (sin suscripciones cargadas todavía, así que no se le disparó ningún push a ningún dispositivo real) — confirmado en `net._http_response` que la Edge Function respondió `200 ok`; dato de prueba borrado. Confirmado también que la función rechaza con `403` una llamada directa sin el secreto correcto. `tsc --noEmit` y `next build` sin errores (sin dev server activo; se excluyó `supabase/functions/` de `tsconfig.json` porque es código Deno, no del proyecto Next.js). **No se probó de punta a punta con un navegador real** (pedir permiso, recibir un push real en un dispositivo) — sin herramienta de browser en esta sesión; recomendado que el usuario loguee con una cuenta real, acepte el permiso, y confirme que le llega un push al asignarle/comentarle/vencerle una tarea. El Personal Access Token de Supabase usado en toda la sesión (y en la rutina cloud fallida del punto anterior) sigue sin revocar — recomendado revocarlo/regenerarlo ahora que terminó el módulo. Con esta sesión se cierran las 4 sesiones del Módulo 7 — proyecto sin módulos pendientes en la tabla del punto 5. |
| 2026-08-27 | Verificación post-cierre (`/supervisor-proyecto`) — push confirmado en dispositivo real | El ✅ de la sesión anterior se apoyaba solo en una prueba sintética (respuesta 200 de la Edge Function con 0 suscripciones reales). Con el usuario probando en producción aparecieron 3 suscripciones reales (`lautaroloza89@gmail.com`, `patinsaintgermain@gmail.com`/Luciana, `lautaroloza88@gmail.com`, PWA instalada en iOS Safari en 2 celulares), pero el aviso no llegaba — la función original siempre respondía `"ok"` sin importar si `web-push` fallaba por suscripción, así que no había forma de ver el error real. Se corrigió `supabase/functions/send-push/index.ts` para que devuelva el detalle de cada intento (`ok`/`statusCode`/`body`/`message` por endpoint) en vez de una respuesta ciega, redesplegada vía Management API (`PATCH /functions/send-push`, ahora versión 3). Con la función nueva se probó en tiempo real contra la cuenta Admin (llamando la Edge Function directo, sin pasar por la tabla `notificaciones`, para no ensuciar datos): la respuesta reportó `ok:true` (Apple aceptó el push) y el usuario confirmó en el momento que la notificación **apareció en el celular real** — infraestructura funcionando de punta a punta, confirmado con un dispositivo real, no solo con la cadena de servidor. El reporte inicial de que a Luciana "no le salía el permiso" no se investigó a fondo por separado (quedó implícitamente resuelto: su suscripción ya figuraba guardada en la base al momento de este chequeo) — si vuelve a fallar específicamente en su cuenta, revisar aparte. **Decisión de alcance confirmada con el usuario:** el push (y las notificaciones en general) siguen cubriendo únicamente tareas (asignación/comentario/vencimiento); no hay notificaciones de turnos/horarios y no se agregan ahora — el usuario preguntó por confusión ("pensé que el push era para cualquier notificación") y decidió dejarlo para después en vez de sumarlo a esta sesión. `tsc --noEmit` sin errores tras el cambio. Pendiente: commitear y pushear este ajuste de `send-push`. |
| 2026-08-29 | Auditoría Fase B, sesión 3/3 — decisiones de producto en DB · Fase B cerrada ✅ | Sesión más delicada de las 3 (`docs/auditoria-fase-b-3-db-decisiones.md`), arrancada solo con las sesiones 1 y 2 ya commiteadas (confirmado con `git status`/`git log` antes de tocar nada) y sin escribir SQL hasta tener las 3 decisiones del usuario. **Hallazgo #6 (`turnos.grupo_nivel` sin catálogo):** el usuario decidió dejarlo como texto libre, sin tabla nueva — con solo 4 valores hoy, sin duplicados, no se justifica la complejidad; sin cambios de esquema, solo documentado en `docs/modelo-datos.md`. **Hallazgo #7 (`notificaciones.tarea_id` cascade):** el usuario delegó la decisión ("lo que vos recomiendes"); se recomendó y aplicó `on delete set null` (mismo patrón que `tareas.created_by`/`turnos.profesor_id`), para que borrar una tarea no destruya el historial de notificaciones de otros usuarios como efecto colateral — migración `20260829150000_notificaciones_tarea_id_set_null.sql`. **Hallazgo #8 (tarea puede quedar sin responsables):** el usuario eligió sumar un aviso; se creó el trigger `notificar_tarea_sin_responsables` (`after delete on tarea_asignados`, security definer) que notifica a los Admins reales cuando una tarea queda con 0 responsables tras un borrado, reusando la tabla `notificaciones` y la campana existentes (confirmado antes de escribir código que `NotificacionesBell.tsx` no distingue por `tipo`, así que no hizo falta tocar UI) — migración `20260829160000_notificar_tarea_sin_responsables.sql`. Ambas migraciones aplicadas contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API y registradas en `supabase_migrations.schema_migrations`. Verificado contra la base real dentro de transacciones con `rollback` (mismo método que sesiones previas, para no generar notificaciones reales ni dejar datos de prueba — 0 filas de prueba restantes, confirmado): el trigger nuevo genera una notificación por cada uno de los 2 Admins reales al quedar una tarea sin responsables; no notifica si la tarea también fue borrada en la misma operación (cascade desde `tareas`); una notificación de otro usuario sobrevive con `tarea_id = null` al borrar la tarea que la originó. `docs/modelo-datos.md` actualizado con el detalle completo de la sesión. No se corrió `tsc --noEmit`/`next build` porque esta sesión no tocó código de la aplicación, solo SQL y docs. El Personal Access Token de Supabase usado en esta sesión sigue sin revocar — recomendado revocarlo/regenerarlo. Con esta sesión se cierran las 3 sesiones de la auditoría Fase B (frontend, DB aditivo, decisiones de producto). |
| 2026-08-29 | Fase 1.2 (Sesión 1) — Notificaciones extendidas a Clases/Turnos: comentario nuevo | Alcance acordado con el usuario antes de escribir código: único disparador de esta sesión es "comentario nuevo en un turno" (no hay "vencimiento" de turno, el horario de un grupo es fijo). Se descubrió al revisar el código que Clases/Turnos no tenía ningún feature de comentarios (a diferencia de Tareas, que ya tenía `tarea_comentarios`) — se confirmó con el usuario (pregunta de alto impacto, sin criterio previo) que esta sesión incluye construirlo desde cero, y que el destinatario del aviso es solo el profesor asignado al turno (`turnos.profesor_id` es 1:1, no M:N como `tarea_asignados`), excluido si es quien comentó. Migración `supabase/migrations/20260829170000_notificaciones_comentario_turno.sql` (aplicada en 3 bloques por el límite de tamaño del endpoint, ver [[project-escuela-patin-supabase]]) crea la tabla `turno_comentarios` (mismo patrón que `tarea_comentarios`: RLS con `select`/`insert` abiertos a cualquier autenticado, igual que la lectura ya abierta de `turnos`; `delete` solo Admin), agrega la columna `notificaciones.turno_id` (`on delete set null`, mismo patrón que `tarea_id`), y el trigger `notificar_comentario_turno_nuevo` (security definer, `after insert on turno_comentarios`) con mensaje `"{Nombre} comentó en el turno: {grupo_nivel}"`. Aplicada contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API y registrada en `supabase_migrations.schema_migrations`. Frontend: nuevos `ComentarioTurnoForm.tsx`/`ComentariosTurnoList.tsx` en `src/components/horarios/` (mismo patrón visual que sus equivalentes de Tareas, sin tocar los archivos de Tareas — Módulo 4 ya cerrado), acción `agregarComentarioTurno` en `horarios/actions.ts`, sección "Comentarios" sumada al detalle del turno (`horarios/[id]/page.tsx`); `NotificacionesBell.tsx` (Módulo 7) extendido para navegar a `/horarios/{turno_id}` cuando la notificación no trae `tarea_id` — cambio explícito autorizado por ser justamente el pedido de esta sesión. Se sumó también `formatFechaHora` a `src/lib/utils/date.ts` (util compartido nuevo, no se tocó la copia local ya existente en `ComentariosList.tsx` de Tareas). El trigger de push (Módulo 7 — Sesión 4) no distingue por `tipo`, así que el aviso nuevo ya se envía por push sin ningún cambio adicional. Verificado con 3 escenarios reales dentro de una transacción con `rollback` (mismo método que sesiones previas — 0 turnos/comentarios/notificaciones de prueba restantes, confirmado): comentario de un autor distinto al profesor asignado notifica correctamente; autocomentario del profesor no genera notificación; turno sin profesor asignado no genera notificación. `tsc --noEmit` y `next build` sin errores (sin dev server activo). No se probó en el navegador (sin herramienta de browser en esta sesión) — recomendado un vistazo manual: comentar un turno ajeno y confirmar que le llega la notificación (campana + push) al profesor asignado. El Personal Access Token de Supabase usado en esta sesión sigue sin revocar — recomendado revocarlo/regenerarlo. Queda pendiente commitear y pushear estos cambios. |
| 2026-08-31 | Sistema PSG, Fase 2 — Sesión 1: modelo de datos base (grupos, grupo_horarios, alumnas, contactos) | Groundwork de Fase 2, sin UI, del que dependen todos los módulos siguientes (F2 MOD 1-4). Migración `supabase/migrations/20260831120000_f2_grupos_alumnas_contactos.sql` (aplicada en 3 bloques por el límite de tamaño del endpoint, ver [[project-escuela-patin-supabase]]) crea 4 tablas: `grupos` (catálogo fijo de 5 grupos/niveles, `cuota_mensual` solo con el valor vigente, sin historial — el historial de lo cobrado queda en F2 MOD 3), `grupo_horarios` (uno o más bloques por grupo, tabla aparte porque Jungla tiene dos; `dias` es `smallint[]` con la convención ISO de `extract(isodow from ...)` — 1=lunes...7=domingo — elegida para que F2 MOD 1 y F2 MOD 4 puedan filtrar/calcular fechas directamente en vez de parsear texto libre), `alumnas` (datos puros, ~150 registros a cargar por import aparte, sin login ni cuenta de Auth, sin relación con el rol "Patinador/a" de Fase 1 a propósito; `grupo_id` FK `not null on delete restrict`, se busca/ordena por `apellido`; descartados a propósito fecha de nacimiento/email/datos médicos/motivo de baja) y `contactos` (uno a varios con `alumnas`, `es_pagador_principal` sin restricción de unicidad, solo precarga default para F2 MOD 3). Datos semilla cargados: los 5 grupos con sus cuotas (vigentes desde septiembre 2026 según comunicado del club) y los 6 bloques horarios (Jungla con sus dos). RLS: `grupos`/`grupo_horarios` lectura abierta a cualquier autenticado, escritura Admin/Head Coach; `alumnas`/`contactos` lectura y escritura exclusiva para Admin, Head Coach y Secretaria (política única `for all` por tabla) — **`'Secretaria'` todavía no es un valor posible de `users.rol`** (Dai sigue en `'Admin'` como parche temporal, sin tocar en esta sesión por estar fuera de alcance según el pedido), la policy quedó escrita contra el rol correcto para que funcione sola cuando ese parche se resuelva aparte. Se encontró y corrigió un bug real durante la verificación: el CHECK de "`dias` no vacío" usaba `array_length(dias,1) > 0`, que en Postgres devuelve `NULL` (no `0`) para un array vacío y por lo tanto no bloqueaba nada — corregido a `cardinality(dias) > 0` (fix aplicado también contra la base real antes de cerrar la sesión). Verificado contra el proyecto real dentro de transacciones con `rollback` (0 filas de prueba restantes, confirmado con conteos antes/después): 8/8 escenarios de constraints (dias vacío, día fuera de rango 1-7, horario inválido, cuota_mensual≤0, DNI duplicado, borrado de grupo con alumnas bloqueado por `restrict`, cascada de contactos al borrar alumna, estado inválido) y 9/9 escenarios de RLS simulados con los IDs reales de Admin/Head Coach/Profesor/Empleado (`set local role authenticated` + `request.jwt.claims`) — Profesor lee grupos pero no puede escribir ni ver alumnas, Head Coach tiene control total sobre ambas, Empleado bloqueado en todo. Tipos TypeScript nuevos en `src/types/grupo.ts` y `src/types/alumna.ts` (sumados a `index.ts`), siguiendo el mismo patrón que `turno.ts`; `tsc --noEmit` sin errores. `docs/modelo-datos.md` actualizado con las 4 tablas nuevas, la nota del bug de `dias`, y la aclaración de que `grupos` (Fase 2) y `turnos.grupo_nivel` (Fase 1, texto libre) son conceptos distintos que no se unificaron en esta sesión. Sin UI construida (fuera de alcance explícito): gestión de grupos, alumnas/contactos (F2 MOD 2), import del listado real de alumnas, y el retoque de `turnos.grupo_nivel` para usar `grupos` por FK (Fase 1.2, Sesión 2, siguiente en la cola). El Personal Access Token de Supabase usado en esta sesión sigue sin revocar — recomendado revocarlo/regenerarlo. Queda pendiente commitear y pushear estos cambios. |
| 2026-08-31 | Sistema PSG, Fase 2 — Sesión 2: Clases/Turnos por FK real + 2 retoques sueltos | Alcance acordado de antemano en 3 partes. **(1) Grupo por FK:** migración `supabase/migrations/20260831130000_turnos_grupo_id_fk.sql` renombra `turnos.grupo_nivel` a `grupo_legacy` (deja de ser `not null`) y agrega `turnos.grupo_id` (FK nullable a `grupos`, `on delete set null`) con índice — sin migración automática de datos (4 filas existentes, las mapea Lauti a mano después). `TurnoForm.tsx` reemplaza el input de texto por un `<select>` de los 5 grupos reales; cuando el grupo tiene un solo bloque horario se deriva y muestra solo lectura, cuando tiene más de uno (caso Jungla, único con dos bloques) aparece un segundo select pidiendo cuál de los dos corresponde — `horarios/actions.ts` (`crearTurno`/`editarTurno`) ya no confía en el horario que manda el formulario: vuelve a consultar `grupo_horarios` por el `grupo_horario_id` elegido y valida que pertenezca al `grupo_id` seleccionado antes de copiar `hora_inicio`/`hora_fin` al turno. `TurnoCard`/listados/detalle muestran `grupos.nombre` cuando el turno ya está mapeado, con fallback a `grupo_legacy` mientras no lo está. El trigger `notificar_comentario_turno_nuevo` (Fase 1.2, Sesión 1) armaba el mensaje leyendo la columna vieja por nombre — se actualizó (`create or replace function`, misma migración) para usar `coalesce(grupos.nombre, turnos.grupo_legacy)`. **(2) Borrar tarea:** `borrarTarea` en `tareas/actions.ts` + `BorrarTareaButton.tsx` (mismo patrón de confirmación que `BorrarTurnoButton`), gateado a Admin solamente a nivel aplicación — mismo criterio que ya regía para borrar un turno; sin migración de RLS porque `tareas_delete` (desde el 2026-08-22) ya lo permitía, aunque de forma más amplia (también Profesor/Head Coach sobre lo propio) — asimetría documentada en `docs/modelo-datos.md`, no un hueco de seguridad. Borrado real (no archivado), decisión ya cerrada en el pedido de la sesión. **(3) Markdown en comentarios:** instalados `react-markdown` + `remark-gfm`; componente nuevo compartido `src/components/ui/ComentarioMarkdown.tsx` (mapea encabezados/listas/tablas/links a los tokens de diseño existentes, sin plugin de tipografía) usado tanto en `ComentariosList.tsx` (Tareas) como en `ComentariosTurnoList.tsx` (Turnos) — el pedido no distinguía cuál de los dos módulos, se aplicó a ambos por consistencia entre las dos vistas de comentarios que comparten el mismo patrón visual; a confirmar con el usuario si el caso de uso real (Luciana pegando planificaciones) es solo en Turnos. Saltos de línea simples preservados con `white-space: pre-line` en vez de sumar `remark-breaks` (no estaba en el pedido de dependencias). Verificado contra la base real: `tsc --noEmit` y `next build` sin errores (sin dev server activo); migración aplicada vía Management API con un Personal Access Token nuevo (pendiente de revocar, ver más arriba) y verificada leyendo `information_schema.columns` y `pg_proc`; el trigger actualizado se probó dentro de una transacción con `rollback` con dos escenarios (turno con `grupo_id` mapeado, turno legacy sin mapear) — ambos armaron el mensaje esperado, 0 filas de prueba restantes confirmado. No se probó en el navegador (sin herramienta de browser en esta sesión) — pendientes de vistazo manual detallados más arriba (punto "Pendientes sueltos"). `docs/modelo-datos.md` actualizado con las 3 partes de la sesión. |
| 2026-08-31 | Verificación post-Sesión 2 (`/supervisor-proyecto`) — incidente en producción: falta de push · corregido ✅ | El usuario reportó no ver turnos en `/horarios` y no ver el botón de borrar tarea recién agregado. Investigado antes de tocar código: `git log origin/main` mostraba el commit de la Sesión 2 (`214f2a9`) sin pushear (`ahead 1`) — la sesión anterior había commiteado localmente pero nunca subido a GitHub. Como Vercel autodeploya desde `origin/main`, el sitio en producción seguía sirviendo el código viejo (commit `8810ac4`), que consulta `turnos.grupo_nivel` — columna que la migración de la Sesión 2 ya había renombrado a `grupo_legacy` en la base real. Confirmado con una request directa a PostgREST (`select=...grupo_nivel...`): `400 { "code": "42703", "message": "column turnos.grupo_nivel does not exist" }`. Esto explica ambos síntomas con una sola causa: la query de turnos fallaba silenciosamente (el código no revisa el `error` de Supabase, así que `data ?? []` mostraba la lista vacía en vez de un error visible) y el botón de borrar tarea no existía en el código desplegado porque ese cambio tampoco se había subido. Sin cambios de código: se re-verificó `tsc --noEmit` y `next build` limpios (sin dev server activo) contra el mismo commit ya probado por la sesión anterior, se confirmó con `git diff --stat origin/main main` que el contenido pendiente de subir coincidía exactamente con lo que esa sesión había declarado (sin sorpresas), y se pusheó (`git push origin main`, confirmado con el usuario antes por ser una acción visible/compartida) — Vercel debería redesplegar en 1-2 minutos. **Lección para sesiones futuras:** cuando una migración de base de datos es un cambio incompatible con el código anterior (ej. un rename de columna, no aditivo), aplicar la migración sin pushear el código correspondiente deja producción rota hasta el próximo push — en esos casos, pushear es parte de cerrar la sesión, no un paso opcional para "después". No se pudo confirmar visualmente en el navegador que Vercel ya redesplegó y que los turnos vuelven a verse (sin herramienta de browser en esta verificación) — pendiente que el usuario lo confirme, ver "Pendientes sueltos". |
| 2026-08-31 | Sistema PSG, Groundwork 3 — arreglos a nivel clase (notificación de asignación, varios profesores por clase, terminología, borrar/cancelar) | Alcance acordado de antemano en 4 partes, todo a nivel de la clase en sí (planificaciones queda para F2 MOD 1). Antes de escribir la migración se preguntó al usuario sobre una ambigüedad de alto impacto: el pedido decía "cada profesor sigue viendo solo lo asignado a él", pero el estado real (decidido en el Módulo 3 y confirmado en el Módulo 6 Dashboard) es lectura ABIERTA a todo el equipo — el usuario confirmó mantener el horario compartido tal cual está, sin restringir por rol; la frase del pedido se satisface solo en el sentido de que la notificación llega a los asignados, no de qué ve cada quien en el listado. **(1) Varios profesores por clase:** `turnos.profesor_id` (1:1) reemplazado por tabla nueva `turno_profesores` (M:N, mismo patrón que `tarea_asignados`) — hay clases con dos profesoras a la vez (Caro/Dai entre semana, Male/Estefi los sábados), confirmado por Lauti. Migración `supabase/migrations/20260831140000_f2_groundwork3_multiprofesor_notif_asignacion.sql` (aplicada en 6 bloques por el límite de tamaño del endpoint, ver [[project-escuela-patin-supabase]]): crea `turno_profesores` con RLS (lectura abierta; insert/delete Admin y Head Coach control total, Profesor solo autoasignarse/quitarse — mismo alcance que ya tenía sobre `profesor_id`), migra los 4 turnos existentes (4/4 verificado con conteo antes/después), reescribe `turnos_insert`/`turnos_update` (ya no pueden condicionar por `profesor_id`; Profesor pasa a validarse con un `exists` contra `turno_profesores` para `update`) y dropea `turnos.profesor_id` (el índice viejo se cae en cascada). Frontend: `AsignadosChecklist` (antes solo en Tareas) generalizado con prop `name` y movido a `src/components/ui/` para reusarlo también en `TurnoForm` (checklist múltiple, visible solo a Admin/Head Coach; un Profesor sigue sin ver el checklist y queda autoasignado, igual que antes); `horarios/actions.ts` (`crearTurno`/`editarTurno`) reescritos para manejar el array `profesores` con el mismo patrón de diff altas/bajas que `editarTarea` (Módulo 7 — Sesión 1), para que el trigger de asignación no reenvíe a quien ya estaba asignado; `TurnoCard`, `horarios/page.tsx`, `dashboard/page.tsx` y el detalle/edición de turno actualizados para consultar `profesores:turno_profesores(profesor:users(...))` (join anidado) en vez de la FK directa vieja. **(2) Notificación de asignación de clase:** hueco real, no bug — en Fase 1.2 el único disparador scopeado fue "comentario nuevo". Trigger nuevo `notificar_turno_asignado` (`after insert on turno_profesores`, security definer, tipo `'turno_asignado'`), no notifica autoasignación, mensaje `"Te asignaron la clase: {grupo} (DD/MM)"`; si la clase tiene varios profesores, cada inserción genera su propia notificación (todos se enteran). El trigger de push (Módulo 7 — Sesión 4) no distingue por `tipo`, así que ya queda cubierto sin cambios adicionales. De paso se actualizó `notificar_comentario_turno_nuevo` (asumía un único `profesor_id`) para notificar a todos los profesores en `turno_profesores`, excluyendo al autor — mismo cambio de cardinalidad que el resto de la sesión. **(3) Terminología:** unificados a "clase" los textos sueltos que seguían diciendo "turno" — botones (Editar/Cancelar/Reactivar/Borrar clase, con sus diálogos de confirmación), títulos de página (Nueva clase, Editar clase, Detalle de clase), mensajes de error y estados vacíos, stat cards del dashboard, y el label de navegación ("Clases / Turnos" → "Clases"). No se tocaron nombres de archivos/componentes/funciones internas (`BorrarTurnoButton`, `crearTurno`, etc.) por ser identificadores no visibles al usuario, fuera del alcance del pedido. **(4) Borrar y Cancelar:** "Borrar clase" ya tenía diálogo de confirmación nativo (`window.confirm`) y estilo rojo (tokens `error-*`) desde el Módulo 5 — no era un hueco, solo le faltaba el texto en "clase". "Cancelar clase" pasó de los mismos tokens rojos a tokens ámbar (`warning-*`, ya existentes en `globals.css`, sin inventar colores nuevos) para distinguirse visualmente de Borrar; "Reactivar" se mantuvo en verde (`success-*`), sin cambios. Ambos siguen en filas separadas, como ya estaban. Verificado contra la base real dentro de transacciones con `rollback` (mismo método que sesiones previas, 0 filas de prueba restantes y rol de la cuenta de prueba devuelto a `Empleado`, confirmado con conteos antes/después): asignación por Head Coach notifica al asignado; agregar un segundo profesor notifica solo al nuevo, no reenvía al primero; autoasignación de un Profesor no notifica; comentario de un tercero notifica a los 2 profesores asignados; comentario de uno de los asignados notifica solo al otro (no se autonotifica); un Profesor puede editar la clase donde está asignado pero no una ajena (0 filas afectadas); un Profesor no puede asignar a otro profesor distinto de sí mismo (bloqueado por RLS). `tsc --noEmit` y `next build` sin errores (sin dev server activo, verificado antes de buildear). `docs/modelo-datos.md` actualizado (tablas `turnos`/`turno_profesores`, sección `notificaciones`, índices, diagrama de relaciones). No se probó en el navegador (sin herramienta de browser en esta sesión) — ver "Pendientes sueltos". El Personal Access Token de Supabase usado en esta sesión sigue sin revocar. Queda pendiente commitear y pushear estos cambios. |
| 2026-08-31 | Parche de retoques varios (post-UI) | Sesión corta de arreglos, no un módulo (tabla del punto 5 sin cambios). Alcance en 3 partes, pedido directamente sin ambigüedad de producto. **(1) Markdown en descripción de tareas:** el componente `ComentarioMarkdown.tsx` (Fase 1.2, Sesión 2) solo se aplicaba a comentarios; la descripción de tareas seguía mostrándose como texto plano (asteriscos crudos visibles). Renombrado a `MarkdownText.tsx` (nombre genérico, ya no exclusivo de comentarios; agregado prop `className` opcional) y usado también en `tareas/[id]/page.tsx` para la descripción — sin dependencias nuevas, `react-markdown`/`remark-gfm` ya estaban instalados. Los dos usos existentes (`ComentariosList.tsx` de Tareas y `ComentariosTurnoList.tsx` de Turnos) actualizados al import nuevo, sin cambio de comportamiento. **(2) Rol de Carolina:** pendiente desde Fase 1.2 (mapeo de roles reales) — seguía en `Empleado` en la base real, corregido a `Profesor` con un `UPDATE` directo vía Management API (sin migración, es dato no esquema), verificado antes y después del cambio. **(3) Validación de fechas en tareas:** función `validarFechas` nueva en `tareas/actions.ts`, usada en `crearTarea` y `editarTarea` (mismo patrón que la validación existente de título obligatorio) — si `fecha_vencimiento < fecha_inicio` devuelve error sin bloquear el resto del formulario (server action, inputs no controlados conservan lo tecleado). No se tocó el dato de la tarea existente con fechas invertidas (Inicio 01/09/2026, Vence 31/08/2026, detectada al probar la UI) — consultado al usuario, prefirió corregirla él mismo desde la app ahora que el formulario avisa. `tsc --noEmit` sin errores (sin dev server activo; no se corrió `next build` completo por la misma razón). No se probó en el navegador (sin herramienta de browser en esta sesión) — pendiente un vistazo manual: abrir una tarea con descripción en varias líneas/lista y confirmar que se ve formateada, y probar el mensaje de error de fechas en el formulario. El Personal Access Token de Supabase usado en esta sesión sigue sin revocar (ver "Pendientes sueltos"). Commiteado (`721176a`), pendiente de pushear. |
| 2026-08-31 | Sistema PSG, Fase 2 — F2 MOD 1: Planificaciones | Luciana arma las planificaciones en ChatGPT y hoy las pega como parche en comentarios (y hasta en tareas, "Planificación Gladiadores Septiembre"); este módulo le da el lugar correcto. Antes de migrar se le preguntó al usuario una decisión de alto impacto (pregunta explícita, sin asumir en silencio): si `planificacion`/`tipo` viven en `turnos` (ya una fila por fecha desde el Módulo 2) o en una tabla nueva separada — eligió reusar `turnos`, así que la carga con checkboxes de fechas crea o actualiza filas de esa tabla en vez de duplicar el concepto. **(1) Esquema:** migración `supabase/migrations/20260831150000_f2_mod1_planificaciones.sql` — `turnos.planificacion` (text, markdown) y `turnos.tipo` (`'Patín'` default / `'Preparación física'`, check), índice `turnos_grupo_id_fecha_idx`; tabla nueva `grupo_objetivos_mes` (grupo_id, mes con check día-1, objetivo, unique(grupo_id, mes)) para el "Objetivo del mes" (punto 6 del pedido — el nivel trimestre no se construyó, apareció una sola vez en lo relevado). RLS de `grupo_objetivos_mes`: lectura abierta, escritura Admin/Head Coach/Profesor/Secretaria (misma convención que `alumnas`/`contactos` para el rol `'Secretaria'` que todavía no existe en `users.rol`). Sin cambios de RLS en `turnos`: `turnos_insert`/`turnos_update` (Groundwork 3) ya permiten Admin/Head Coach/Profesor. **(2) Backend:** `horarios/planificaciones-actions.ts` nuevo con `guardarPlanificacion` (upsert por `grupo_id`+`fecha`: si existe el turno solo actualiza `tipo`/`planificacion`, si no lo crea derivando `hora_inicio`/`hora_fin` del bloque de `grupo_horarios` que cubre el día ISO de esa fecha), `duplicarPlanificacion` (mismo upsert, una sola fecha) y `guardarObjetivoMes` (upsert grupo+mes). Utilidades nuevas en `lib/utils/date.ts` (`fechasDelMesPorDia`, `diaIsoDeFecha`, `primerDiaDeMes`, `nombreDia`, `nombreMes`) sin dependencias nuevas. **(3) Frontend:** `TurnoForm` gana tipo + textarea de planificación; `/horarios/[id]` muestra tipo (solo si es "Preparación física", discreto junto al grupo — la excepción que hay que distinguir) y la sección Planificación renderizada en markdown entre el encabezado y los comentarios (orden pedido), más "Duplicar a otra fecha" (`/horarios/[id]/duplicar`). Navegación nueva grupo → mes → planificaciones: `/horarios/grupos` (los 5 grupos) → `/horarios/grupos/[id]` (selector de mes vía `?mes=`, "Objetivo del mes" editable, planificaciones del mes separadas por tipo) → `/horarios/grupos/[id]/planificar` (elegís día de semana entre los configurados en `grupo_horarios` del grupo, aparecen las fechas de ese mes con ese día tildadas por default — cubre tanto el caso "por día de semana" como "por semana" alternando qué fechas se destildan, sin config extra). Nav renombrado "Clases" → "Planificaciones" (`AppHeader`); la URL sigue siendo `/horarios` y los identificadores internos (`TurnoForm`, `crearTurno`, etc.) no se tocaron, mismo criterio que el rename de Groundwork 3. La lista plana existente en `/horarios` (agenda cronológica con filtro de estado) se mantiene sin cambios de fondo, con un link nuevo "Ver por grupo y mes →". Detectado y corregido durante el desarrollo: `ObjetivoMesForm` importaba `MarkdownText` (react-markdown) del lado cliente, agregando ~44kB innecesarios al bundle de `/horarios/grupos/[grupoId]`; se resolvió pasándole la vista ya renderizada en el servidor como prop `vista` en vez de importar el renderer en el componente cliente (bundle bajó de 45.1kB a 1.38kB en esa ruta, confirmado con `next build`). **Verificado** contra el proyecto real (`gbnpebqcobtoegeagmcl`) dentro de una transacción con `rollback`: tipo inválido rechazado (check), default `tipo='Patín'`, texto con tildes/emoji preservado en `planificacion` ("Preparación física" incluido), `mes` no-día-1 rechazado (check), duplicado `grupo_id`+`mes` rechazado (unique), Empleado bloqueado por RLS al escribir en `grupo_objetivos_mes`, Profesor sí puede — 7/7 escenarios OK, 0 filas de prueba restantes confirmado. Migración registrada en `supabase_migrations.schema_migrations`. `tsc --noEmit` y `next build` sin errores (sin dev server activo). `docs/modelo-datos.md` actualizado (tablas `turnos`/`grupo_objetivos_mes`, diagrama de relaciones). Avisar a Luciana que deje de cargar planificaciones como tareas o dentro de comentarios ahora que este módulo existe (los casos ya cargados así los mueve ella a mano, no se migran por código). No se probó en el navegador (sin herramienta de browser en esta sesión) — ver "Pendientes sueltos". El Personal Access Token de Supabase usado en esta sesión sigue sin revocar. Queda pendiente commitear y pushear estos cambios. |
| 2026-08-31 | Parche — unificar creación de planificaciones | Sesión corta de corrección sobre F2 MOD 1, no un módulo nuevo (tabla del punto 5 sin cambios). Habían quedado dos formularios de creación divergentes: "Nueva clase" (fecha única + profesores, sin fechas múltiples) y el de `/horarios/grupos/[grupoId]/planificar` (día de semana + fechas múltiples, sin profesores). **(1)** Eliminados la ruta `/horarios/nuevo`, el botón "Nueva clase" en `/horarios` y la acción `crearTurno` — la única forma de crear una planificación pasa a ser grupo → mes → planificar. **(2)** `PlanificarForm` (el formulario unificado) suma el checklist de profesores (`AsignadosChecklist`, mismo criterio de permisos que tenía "Nueva clase": Admin/Head Coach eligen, Profesor autoasignado); `guardarPlanificacion` sincroniza los profesores elegidos contra cada fecha tildada usando el mismo diff altas/bajas que ya usaba `editarTurno` (para que el trigger de notificación de asignación no reenvíe a quien ya estaba). **(3)** Título "Nueva planificación" (ya lo tenía) y "Tipo de clase" → "Tipo de planificación" en ese formulario; el resto de la terminología de la app ("Editar clase", "Cancelar clase") queda igual, fuera del alcance de este parche. **(4)** Día de semana sin cambios (solo ofrece los días configurados en `grupo_horarios` del grupo — se evaluó ofrecer los 7 días para "recuperar" clases y se descartó, este club no tiene clases de recuperación). **(5)** Sacado el selector manual de bloque horario (caso Jungla, dos bloques) tanto de la creación como de "Editar clase" — el horario se deriva siempre del día ISO de la fecha elegida. Refactor de soporte: dos helpers compartidos nuevos, `src/lib/horarios/resolver-horario.ts` (`resolverHorarioPorDia`, ya existía duplicado en `actions.ts` y `planificaciones-actions.ts`) y `src/lib/horarios/sync-profesores.ts` (`sincronizarProfesores`, extraído de la lógica que ya tenía `editarTurno`), usados ahora por `editarTurno`, `guardarPlanificacion` y `duplicarPlanificacion`. `TurnoForm` simplificado: sin prop `modo` (queda solo para editar, ya no crea), sin selector de bloque — la fecha pasa a ser un input controlado para poder derivar y mostrar el horario en vivo. **Efecto colateral de compartir el sincronizador:** `duplicarPlanificacion` ahora también copia los profesores de la clase de origen (antes quedaba sin profesor asignado) — no pedido explícitamente, pero consecuencia directa de unificar la lógica y coherente con lo que se espera de "duplicar". **Nota de comportamiento a tener en cuenta:** si se recarga una planificación para fechas que ya tenían profesores asignados, los tildados en ese envío reemplazan a los anteriores (si no se tilda ninguno, quedan sin profesor); para tocar solo el contenido de una fecha ya creada sin resubmitir profesores conviene usar "Editar clase" desde esa clase puntual. Sin cambios de esquema ni de RLS — parche puramente de frontend/acciones. `docs/modelo-datos.md` actualizado con el detalle. `tsc --noEmit` y `next build` sin errores (sin dev server activo; se limpió `.next` por una referencia obsoleta a la ruta `/horarios/nuevo` borrada). No se probó en el navegador (sin herramienta de browser en esta sesión) — ver "Pendientes sueltos". |
| 2026-09-01 | Parche — entrada de Planificaciones + notificaciones | Sesión corta de retoques de UI sobre módulos ya construidos, sin tocar la base de datos (sin migración, sin cambios de RLS; tabla del punto 5 sin cambios). **(1) Entrada unificada del módulo:** `/horarios` abría en una grilla con las planificaciones de todos los grupos mezcladas (ordenadas solo por fecha) más un link "Ver por grupo y mes →" a la vista que realmente se usa — dos pantallas donde va una. Se eliminaron la grilla de tarjetas y ese link, y `/horarios` pasó a ser el selector de grupo (el contenido de `/horarios/grupos`, cuya `page.tsx` se borró; las rutas hijas `/horarios/grupos/[grupoId]` y `.../planificar` no se movieron, para no tocar los links y `revalidatePath` ya existentes). La entrada del módulo es ahora grupo → mes → planificaciones. **(2) Filtros de estado adentro:** Todas/Activo/Cancelado se movieron de `/horarios` a `/horarios/grupos/[grupoId]` (`?estado=` junto al `?mes=` ya existente), donde filtran la lista acotada del grupo y el mes elegidos; el filtro se aplica en la query (`.eq("estado", ...)`), los links de mes anterior/siguiente conservan el estado elegido y los estados vacíos cambian de texto según el filtro. `FiltroEstadoTurnoTabs` se generalizó con props `basePath`/`params` (antes hardcodeaba `/horarios`) y las planificaciones canceladas muestran el `EstadoTurnoBadge` en la lista, para que el filtro "Todas" se pueda leer. **(3) Marcar todas como leídas:** botón nuevo en el panel de la campana (visible solo si hay sin leer), con acción `marcarTodasNotificacionesLeidas` en `src/components/notificaciones/actions.ts` — marca `leida = true` para las sin leer del usuario logueado (filtra por `usuario_id` del `auth.getUser()` además de la RLS). **(4) Contador en la campanita:** el puntito rojo se reemplazó por la cantidad de notificaciones sin leer (nada si es 0, `99+` como tope). El contador es estado aparte de la lista, porque la lista trae solo las últimas 20 y el contador tiene que contar todas: se inicializa con un `count: "exact", head: true` sobre `leida = false`, se incrementa con el mismo canal de Realtime que ya alimenta la lista (INSERT filtrado por `usuario_id`), se decrementa al abrir una notificación y va a 0 al marcar todas. **Efectos colaterales del punto 1, no pedidos pero consecuencia directa:** el "volver" del detalle de una clase apuntaba a la grilla eliminada — ahora vuelve al mes de su grupo (`/horarios/grupos/[grupo_id]?mes=YYYY-MM`), con fallback a `/horarios` para las clases viejas sin `grupo_id` mapeado; y el link "Ver todas" de "Próximas clases" en el Dashboard pasó a decir "Ver por grupo", porque ya no lleva a una lista de todas. Sin cambios en `TurnoCard` (lo sigue usando el Dashboard). `tsc --noEmit` y `next build` sin errores (build limpio tras borrar `.next`; la ruta `/horarios/grupos` ya no aparece en el listado de rutas). No se probó en el navegador (sin herramienta de browser en esta sesión). `docs/modelo-datos.md` actualizado con el cambio de navegación. |
| 2026-09-02 | Sistema PSG, Fase 2 — F2 MOD 2: Alumnas | Antes de construir se detectó una discrepancia real entre lo que Groundwork 1 (20260831120000) dejó aplicado y lo que pedía este módulo — `alumnas.dni` NOT NULL UNIQUE, `alumnas.grupo_id` NOT NULL y `contactos.relacion` NOT NULL, los tres probados a propósito en esa sesión, contra un pedido de dejarlos opcionales en base para tolerar el import futuro de las ~150 alumnas de Luciana (filas incompletas, DNI repetidos). Se preguntó al usuario (pregunta de alto impacto, no se asumió en silencio): eligió relajar los 3 campos. Migración `supabase/migrations/20260902120000_f2_mod2_alumnas_ajustes_esquema.sql` aplicada contra el proyecto real (tabla `alumnas` seguía en 0 filas, sin riesgo de datos): `dni` pasa a nullable con índice único parcial `alumnas_dni_unique_idx` (`where dni is not null`, reemplaza el unique total `alumnas_dni_key`), `grupo_id` pasa a nullable (sigue obligatorio a nivel de formulario, no de base), `fecha_inscripcion` gana default `current_date`, `contactos.relacion` pasa a nullable. Verificado leyendo `information_schema.columns` antes de registrar la migración en `supabase_migrations.schema_migrations`. RLS no se tocó: Groundwork 1 ya había dejado `alumnas`/`contactos` con policies `for all` completas para Admin/Head Coach/Secretaria (contrario a lo que el MD del módulo asumía sobre un "esqueleto sin políticas" — se verificó el estado real antes de escribir código, no se asumió). **Tipos:** `Rol` (`src/types/user.ts`) suma `'Secretaria'` (todavía no es un valor real de `users.rol`, mismo criterio que la RLS de Groundwork 1 — el código de permisos ya compila contra el rol antes de que exista, sin necesitar otro cambio cuando se resuelva el parche de Dai); `Alumna`/`Contacto` (`src/types/alumna.ts`) actualizados a `dni`/`grupo_id`/`relacion` opcionales. **Navegación:** sección nueva "Administración" en `AppHeader.tsx` (no existía ningún precedente de filtro de nav por rol; se agregó un bloque condicional aparte del array `ENLACES` plano ya existente), visible solo para Admin/Head Coach/Secretaria, con un único ítem "Alumnas" por ahora. `middleware.ts` protege `/alumnas` por sesión (como el resto de las rutas); el bloqueo por rol es server-side dentro de cada página (`redirect("/dashboard")` si el rol no corresponde), mismo patrón ya usado en Tareas/Horarios — no existía precedente de gate por rol en el middleware mismo. **Listado** (`/alumnas`): sin precedente de buscador ni de filtro combinado en toda la app — se resolvió trayendo las ~150 filas completas del server (sin paginación, según el pedido) y filtrando en un client component nuevo (`AlumnasListClient.tsx`) que combina búsqueda de texto (normalizada sin mayúsculas/acentos vía `normalize("NFD")`), filtro de grupo y tabs de estado (Activas por default, clon visual de `FiltroEstadoTurnoTabs` pero con `onClick`/estado de React en vez de `Link`+query params, porque los 3 filtros se combinan en memoria) — contador "Mostrando X de Y", y las dos variantes de vacío (real vs. por filtro) que pedía el módulo. **Alta/edición** (`AlumnaForm.tsx`): sin precedente de repetidor de filas dinámicas en la app — contactos inline con `useState`, radio-like exclusivo para "pagador principal" resuelto con checkboxes controlados por React (al marcar uno se desmarcan los demás en el estado, no por constraint de base) y auto-marcado cuando queda un solo contacto (`useEffect` sobre `contactos.length`). Server actions (`alumnas/actions.ts`): `crearAlumna` (estado fijo en `'activa'`, no lo elige el formulario) y `editarAlumna` (estado editable, sincroniza contactos con un diff altas/bajas/updates contra lo ya guardado, mismo criterio conceptual que `sincronizarProfesores` de Horarios pero escrito para esta entidad). DNI duplicado valida con un `select` previo por `dni` (excluyendo la propia alumna en edición) y devuelve el apellido/nombre de la alumna existente en el mensaje de error, en vez de un error genérico de constraint. **Ficha** (`/alumnas/[id]`): página propia (no modal), mismo patrón que Horarios/Tareas; contactos listados con el pagador principal ordenado primero y con badge visual. **Verificado en el navegador** con Playwright (paquete instalado aparte en el scratchpad de la sesión, no en el proyecto — no había Playwright ni `chromium-cli` disponibles) contra las 4 cuentas reales (Admin, Head Coach, Secretaria vía su rol real `Admin` parcheado, y Profesor con Keyla): Admin/Head Coach/Secretaria ven "Administración → Alumnas" y pueden entrar; alta con 2 contactos (el primero se automarca pagador principal al quedar solo, se desmarca solo al marcar el segundo); aparece en el listado con el contador correcto; buscador encuentra "Sofía" tipeando "sofia" sin tilde; editar y cambiar de grupo persiste; dar de baja saca a la alumna del filtro "Activas" (default) y la deja en "Bajas"; reactivar la devuelve; alta con DNI ya usado rechazada con el mensaje `"Ya existe una alumna con ese DNI: Pérez Test, Sofía Test"`; un Profesor no ve "Administración" en el menú y es redirigido a `/dashboard` al pegar `/alumnas` directamente; layout mobile (iPhone 13) revisado en listado, menú, ficha y alta — colapsa a una columna, sin overflow. Sin errores de consola en ninguna de las corridas. Datos de prueba borrados al terminar (confirmado 0 filas en `alumnas`/`contactos`). `tsc --noEmit` y `next build` sin errores (sin dev server activo al buildear; se detuvo el dev server levantado para las pruebas antes de cerrar). El Personal Access Token de Supabase usado en esta sesión sigue sin revocar (ver más arriba). |
| 2026-09-02 | Sistema PSG, Fase 2 — F2 MOD 3: Pagos y cuotas | Decisiones de alcance (pago parcial permitido, recargo fijo de $10.000 como checkbox sugerido/editable, sin campo de comprobante) ya habían quedado cerradas en una conversación aparte antes de esta sesión, documentadas en el propio pedido. Antes de escribir la migración se releyó el esquema real de `alumnas`/`contactos`/`grupos` (F2 MOD 1 y 2, ya cerrados) en vez de asumir por el MD: confirmado que `alumnas.grupo_id` y `dni` son nullable (F2 MOD 2) y que `'Secretaria'` todavía no es un valor real de `users.rol` (Dai sigue parcheada en `'Admin'`). **Esquema:** migración `supabase/migrations/20260902130000_f2_mod3_pagos.sql` — tabla `pagos` (snapshot de `monto_cuota` al momento del pago, `monto_recargo` explícito por evento, `monto` = lo efectivamente cobrado en ese evento puntual, `estado` `pendiente_verificar`/`verificado`, `alumna_id` `on delete restrict` y `registrado_por` `on delete restrict` para no perder historial financiero en silencio, `contacto_id`/`verificado_por` `on delete set null`) y `pagos_metodos` (desglose por método, `on delete cascade` desde `pagos`); constraint agregado no pedido explícitamente pero de bajo riesgo, `pagos_verificacion_consistente` (`verificado_por`/`verificado_en` van siempre juntos con `estado='verificado'`), para blindar contra una corrección manual a medio hacer directo en la base (única vía de edición prevista para pagos ya verificados, fuera de alcance de la UI). RLS: política única `for all` para Admin/Head Coach/Secretaria en ambas tablas, mismo patrón que `alumnas`/`contactos` (F2 MOD 1) — `'Secretaria'` de nuevo escrita de antemano contra un rol que todavía no existe en el CHECK real. **Reglas de negocio compartidas** (`src/lib/pagos/reglas.ts`, `src/lib/pagos/saldo.ts`, `src/lib/pagos/recibo.ts`): recargo sugerido cuando ya pasó el día 10 del mes correspondiente y queda saldo sin él (checkbox editable, no un campo separado); "hoy" para estas reglas se calcula en huso horario Argentina (`America/Argentina/Buenos_Aires`, vía `toLocaleDateString("en-CA", …)`) para no correr el corte un día según dónde corra Vercel; el cálculo de saldo de una alumna+mes se comparte entre la sugerencia en vivo del formulario de alta y el reporte de Deudoras (bulk, para no hacer ~150 consultas individuales). **Backend** (`src/app/(dashboard)/pagos/actions.ts`): `crearPago` (valida métodos y los suma a `monto`, autocompleta `monto_cuota` desde `grupos.cuota_mensual` de la alumna, rechaza si la alumna no tiene grupo asignado), `marcarPagoVerificado` (única transición, arma el texto del recibo en el momento recién después de verificar, nunca antes) y `obtenerSaldoAlumnaMes` (callable directo desde cliente, no atado a un `<form>`, para la sugerencia en vivo). **Frontend:** `/pagos` (menú), `/pagos/nuevo` (buscador de alumna por apellido/nombre normalizado sin tildes — mismo criterio que el buscador de Alumnas, aunque no se compartió el componente porque uno filtra una lista completa y el otro selecciona una entre resultados; repetidor de métodos igual al patrón de contactos de `AlumnaForm`; saldo pendiente informativo antes de guardar), `/pagos/pendientes` (filtro por mes con el mismo nav prev/anterior que ya usaba Horarios — extraído a `mesAnteriorSiguiente`/`mesQuery` en `lib/utils/date.ts` como utils nuevos, sin tocar el archivo de Horarios ya cerrado; "Marcar como verificado" resuelto como botón por fila con `useTransition`, mismo patrón que `ToggleEstadoTurnoButton`, que al confirmar reemplaza el botón por el recibo con "Copiar" vía Clipboard API en el momento, sin navegar a otra página), `/pagos/recaudacion` (total + desglose por método, agregado en JS sobre filas ya traídas, mismo criterio que el resto de la app para datasets chicos) y `/pagos/deudoras` (banner de recordatorio los días 8-9 del mes actual, sin cron ni push nuevos). Sección "Administración" de `AppHeader.tsx` suma "Pagos" junto a "Alumnas"; `middleware.ts` suma `/pagos` a las rutas protegidas por sesión (el gate por rol sigue siendo server-side en cada página, mismo patrón que Alumnas). Refactor chico de paso: `AlumnasListClient.tsx` pasó a usar el `normalizarTexto` nuevo de `src/lib/utils/texto.ts` en vez de su copia local idéntica (sin cambio de comportamiento). **Verificado contra el proyecto real** (`gbnpebqcobtoegeagmcl`): 8/8 constraints (día 1 del mes, `monto_cuota`/`monto_recargo`/`monto` > 0 o >= 0, `estado` y `metodo` inválidos, `pagos_verificacion_consistente`) y RLS con los IDs reales de Admin/Head Coach/Profesor/Empleado, todo dentro de transacciones con `rollback` (0 filas de prueba restantes, confirmado con conteos antes/después) — Admin y Head Coach insertan y ven, Profesor y Empleado bloqueados en ambas operaciones. **Verificado en el navegador** con Playwright (mismo método que F2 MOD 2, instalado aparte en el scratchpad — sigue sin estar disponible `chromium-cli`) contra la única alumna real cargada hasta ahora (Juana Luna, grupo "Nivel inicial", cuota $45.000, con un contacto pagador principal real precargado por el formulario): flujo completo de pago parcial (mamá $20.000) → segundo pago completando el mes (transferencia $25.000) → verificar ambos → recibo del primero con "Saldo pendiente del mes: $25.000", recibo del segundo sin línea de saldo → Recaudación del mes suma $45.000 con desglose Efectivo/Transferencia correcto → Deudoras del mes ya no lista a la alumna; por separado, un pago cargado para un mes pasado (agosto) tildó solo el checkbox de recargo automáticamente (sin pagos verificados de ese mes y ya pasado el día 10), y Deudoras de agosto la lista con saldo $45.000 y "18 días de atraso" (hoy 2026-09-02 respecto del día 15 de agosto); Head Coach y Secretaria (Dai, rol real `Admin`) confirmados con acceso real al menú y a `/pagos`; mobile (390×844) revisado en alta y Deudoras, sin overflow. Sin errores de consola en ninguna corrida. Datos de prueba (3 pagos reales sobre Juana Luna) borrados al cerrar (confirmado 0 filas en `pagos`/`pagos_metodos`). No se probó con una cuenta Profesor/Empleado real por no tener esa contraseña a mano en esta sesión — el gate replica exactamente el patrón ya probado de Alumnas y además está reforzado por el RLS ya verificado por SQL, pero queda como hueco menor de verificación. Tampoco se simuló en vivo el banner de recordatorio de Deudoras (depende de que "hoy" sea día 8 o 9; no se forzó el reloj del sistema) — su lógica de fecha es la misma ya probada para el recargo, mismo criterio de riesgo bajo. `tsc --noEmit` y `next build` sin errores (sin dev server activo al buildear). `docs/modelo-datos.md` actualizado con las tablas `pagos`/`pagos_metodos`, el diagrama de relaciones y las reglas de negocio compartidas. El Personal Access Token de Supabase usado en esta sesión sigue sin revocar (ver más arriba). |
| 2026-09-02 | Verificación post-sesión F2 MOD 3 — Profesor/Empleado bloqueados | El usuario pasó las credenciales de Carolina (`carolina@gmail.com`, rol real `Profesor`) y Estefania (`estefania@gmail.com`, rol real `Empleado`), que faltaban al cerrar F2 MOD 3. Con Playwright, mismo método que el resto de la sesión: ninguna de las dos ve "Administración" ni "Pagos" en el menú, y pegar directo cualquiera de las 5 rutas del módulo (`/pagos`, `/pagos/nuevo`, `/pagos/pendientes`, `/pagos/recaudacion`, `/pagos/deudoras`) redirige a `/dashboard` en los dos casos. Sin cambios de código — solo cerraba un hueco de verificación. Con esto, F2 MOD 3 queda con las 4 combinaciones de rol probadas en vivo (Admin, Head Coach, Secretaria-vía-Admin con acceso; Profesor y Empleado sin acceso). |
| 2026-09-03 | Sistema PSG, Fase 2 — F2 MOD 4: Asistencia | Decisiones de alcance (marcado por defecto vacío, definición de la alerta por semanas y no por clases, edición libre de fechas pasadas) ya venían cerradas de una conversación aparte, documentadas en el pedido de la sesión. Antes de escribir la migración se releyó el esquema real de `alumnas`/`contactos` (F2 MOD 2) y `grupos`/`grupo_horarios` (Groundwork 1) en vez de asumirlo del MD: confirmado que `alumnas.grupo_id` es nullable desde F2 MOD 2, que `grupo_horarios.dias` es `smallint[]` con la convención ISO, y que `'Secretaria'` sigue sin ser un valor válido de `users.rol` (Dai parcheada en `'Admin'`). **Esquema:** migración `supabase/migrations/20260903120000_f2_mod4_asistencia.sql` — tabla `asistencia` (`alumna_id` y `registrado_por` con `on delete restrict` para no perder historial en silencio, mismo criterio que `pagos`; `grupo_id` **snapshot** `not null` del grupo en el que estaba la alumna ese día, nunca derivado de `alumnas.grupo_id` al leer; `presente` booleano sin tercer estado porque la justificación de ausencias quedó fuera de alcance), constraint `unique (alumna_id, fecha)` (una fila por alumna por día, que es lo que hace que volver a guardar una fecha actualice en vez de duplicar) y constraint `asistencia_sin_sabados` (`extract(isodow from fecha) <> 6`) — este último no estaba pedido explícitamente como constraint de base, pero el filtro de sábados sí, y se agregó como tercera capa (la UI nunca lista un sábado, la server action lo rechaza, y el check es la red de seguridad) porque el bloque de sábado de Jungla existe de verdad en `grupo_horarios` y es fácil de colar por URL. Índices compuestos `(grupo_id, fecha)` y `(alumna_id, fecha)`, uno por cada una de las dos lecturas del módulo. RLS: política única `for all` para Admin/Head Coach/Secretaria, mismo patrón que `alumnas`/`contactos` y `pagos` — `'Secretaria'` otra vez escrita de antemano contra un rol que todavía no existe en el CHECK real. **Lógica compartida** (`src/lib/asistencia/`): `fechas.ts` reusa `fechasDelMesPorDia` de Planificaciones (F2 MOD 1) y le suma el filtro de sábados, que vive acá y no en `lib/utils/date.ts` justamente porque es exclusivo de Asistencia — las planificaciones de sábado de Jungla siguen funcionando igual; `alertas.ts` calcula la alerta de inasistencias; `consultas.ts` centraliza la lectura paginada de `asistencia` (de a 1000 filas, porque una asistencia diaria de todo el club pasa el límite por defecto del API) y tira error en vez de devolver una lectura a medias, que haría que la alerta muestre menos alumnas de las que corresponde; `permisos.ts` tiene el gate de rol, fuera de `actions.ts` porque un archivo `"use server"` solo puede exportar funciones async. Refactor chico de paso: `hoyArgentina()` (huso Argentina, para no correr un día el corte según dónde corra Vercel) subió de `lib/pagos/reglas.ts` a `lib/utils/date.ts` para que Asistencia la reuse, junto con dos helpers nuevos (`sumarDias`, `lunesDeLaSemana`) — misma implementación, sin cambio de comportamiento en Pagos. **Alerta de inasistencias:** 3 semanas calendario consecutivas (lunes a domingo) sin ningún `presente`, no 3 clases sueltas — contar clases daría un umbral distinto por grupo, porque cada uno tiene 2 o 3 días de clase. La semana en curso no se cuenta (evita falsas alarmas a mitad de semana); una semana solo cuenta si tiene asistencia ya tomada, así que feriados y semanas todavía sin cargar se saltean sin sumar ni romper la racha. Dos criterios que el pedido no fijaba y se resolvieron acá, los dos hacia el lado de no inventar alertas: (a) "semana con asistencia tomada" se evalúa sobre las filas de la propia alumna y no sobre las del grupo — equivalente en el caso normal, porque el guardado en bloque crea una fila por cada alumna activa, pero evita marcar como ausente a una alumna que todavía no estaba en el grupo esas semanas; (b) si la alumna ya tuvo un presente en la semana en curso queda fuera de la alerta aunque venga de una racha de 3, porque ya volvió. Ventana de lectura de 16 semanas, de las cuales las 3 últimas deciden la alerta y el resto sirve para poder decir desde cuándo no viene. Reporte que se consulta (`/asistencia/alertas`), sin notificación ni cron — mismo criterio que "Deudoras". **UI:** `/asistencia` (menú: alertas + selector de grupo), `/asistencia/grupos/[grupoId]` (navegación por mes idéntica a la de Planificaciones, lista las fechas de clase del mes con ✓ Cargada / Pendiente / "Todavía no fue la clase" y un contador de cuántas faltan, más un aviso de confirmación al volver de un guardado), `/asistencia/grupos/[grupoId]/[fecha]` (alumnas activas ordenadas por apellido, arranca sin nadie tildado, checkbox de 20px dentro de un `<label>` de 56px de alto para el pulgar en mobile, contador vivo de presentes/ausentes, un solo submit) y `/asistencia/alertas` (nombre, grupo, semanas de racha, última vez presente y el contacto principal como link `tel:` para llamar directo). `AppHeader.tsx` suma "Asistencia" a la sección Administración y `lib/supabase/middleware.ts` suma `/asistencia` a las rutas protegidas por sesión (el gate por rol sigue siendo server-side por página, mismo patrón que Alumnas y Pagos). Tipo nuevo `src/types/asistencia.ts`, sumado a `index.ts`. **Verificación.** Esta sesión no tuvo credenciales de Supabase ni variables de entorno de la app, así que **no se aplicó la migración contra el proyecto real ni se probó en el navegador** — queda como pendiente explícito (ver punto 7). En su lugar se levantó un Postgres 16 local y se aplicó **la cadena completa de migraciones desde cero** (saltando solo las dos que necesitan `pg_cron`/`pg_net`, que no existen fuera de Supabase y no tocan Asistencia): 6/6 escenarios de esquema — guardado en bloque con presentes y ausentes en la misma operación, re-guardado de la misma fecha que actualiza sin duplicar (2 filas antes y después, valores invertidos), segunda fila para la misma alumna+fecha rechazada por el unique, sábado 2026-09-05 rechazado por el check, snapshot de grupo intacto después de mover a la alumna a otro grupo, y borrado de alumna con historial bloqueado por el `restrict` — y RLS real con `set local role authenticated` + `request.jwt.claim.sub`: Profesor ve 0 filas y su INSERT lo rechaza la policy; Head Coach ve todo, inserta y edita. La lógica de fechas y de la alerta se probó aparte con un cliente Supabase falso y datos armados relativos a la fecha de hoy: sábado excluido siempre (incluido el caso Jungla, y un grupo solo-sábado que queda sin fechas), alerta a las 3 semanas seguidas sin presente, una semana con presente rompe la racha, un feriado en el medio no la rompe ni la suma, 2 semanas no alcanzan, y un presente en la semana en curso saca de la alerta. `tsc --noEmit` y `next build` sin errores (`next lint` no está configurado en el repo — arranca el asistente interactivo de setup de ESLint, se dejó como estaba). `docs/modelo-datos.md` actualizado con la tabla `asistencia`, sus índices, el diagrama de relaciones y la definición de la alerta. **Fuera de alcance, respetado:** asistencia de sábados, baja automática por inasistencia, notificaciones/cron para la alerta, justificación de ausencias, y reportes históricos más allá de la alerta. |
