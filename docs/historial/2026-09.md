# Historial de sesiones — Septiembre 2026

> Archivo histórico. Log de sesiones movido desde el punto 8 de `PROGRESS.md` (reorganización de documentación, 2026-09-08), sin resumir ni recortar: las filas están tal cual se escribieron en su momento.
> El estado actual de cada módulo vive en la tabla de `PROGRESS.md`; las decisiones de producto vigentes, en `docs/decisiones.md`.

| Fecha | Módulo trabajado | Resultado |
|---|---|---|
| 2026-09-01 | Parche — entrada de Planificaciones + notificaciones | Sesión corta de retoques de UI sobre módulos ya construidos, sin tocar la base de datos (sin migración, sin cambios de RLS; tabla del punto 5 sin cambios). **(1) Entrada unificada del módulo:** `/horarios` abría en una grilla con las planificaciones de todos los grupos mezcladas (ordenadas solo por fecha) más un link "Ver por grupo y mes →" a la vista que realmente se usa — dos pantallas donde va una. Se eliminaron la grilla de tarjetas y ese link, y `/horarios` pasó a ser el selector de grupo (el contenido de `/horarios/grupos`, cuya `page.tsx` se borró; las rutas hijas `/horarios/grupos/[grupoId]` y `.../planificar` no se movieron, para no tocar los links y `revalidatePath` ya existentes). La entrada del módulo es ahora grupo → mes → planificaciones. **(2) Filtros de estado adentro:** Todas/Activo/Cancelado se movieron de `/horarios` a `/horarios/grupos/[grupoId]` (`?estado=` junto al `?mes=` ya existente), donde filtran la lista acotada del grupo y el mes elegidos; el filtro se aplica en la query (`.eq("estado", ...)`), los links de mes anterior/siguiente conservan el estado elegido y los estados vacíos cambian de texto según el filtro. `FiltroEstadoTurnoTabs` se generalizó con props `basePath`/`params` (antes hardcodeaba `/horarios`) y las planificaciones canceladas muestran el `EstadoTurnoBadge` en la lista, para que el filtro "Todas" se pueda leer. **(3) Marcar todas como leídas:** botón nuevo en el panel de la campana (visible solo si hay sin leer), con acción `marcarTodasNotificacionesLeidas` en `src/components/notificaciones/actions.ts` — marca `leida = true` para las sin leer del usuario logueado (filtra por `usuario_id` del `auth.getUser()` además de la RLS). **(4) Contador en la campanita:** el puntito rojo se reemplazó por la cantidad de notificaciones sin leer (nada si es 0, `99+` como tope). El contador es estado aparte de la lista, porque la lista trae solo las últimas 20 y el contador tiene que contar todas: se inicializa con un `count: "exact", head: true` sobre `leida = false`, se incrementa con el mismo canal de Realtime que ya alimenta la lista (INSERT filtrado por `usuario_id`), se decrementa al abrir una notificación y va a 0 al marcar todas. **Efectos colaterales del punto 1, no pedidos pero consecuencia directa:** el "volver" del detalle de una clase apuntaba a la grilla eliminada — ahora vuelve al mes de su grupo (`/horarios/grupos/[grupo_id]?mes=YYYY-MM`), con fallback a `/horarios` para las clases viejas sin `grupo_id` mapeado; y el link "Ver todas" de "Próximas clases" en el Dashboard pasó a decir "Ver por grupo", porque ya no lleva a una lista de todas. Sin cambios en `TurnoCard` (lo sigue usando el Dashboard). `tsc --noEmit` y `next build` sin errores (build limpio tras borrar `.next`; la ruta `/horarios/grupos` ya no aparece en el listado de rutas). No se probó en el navegador (sin herramienta de browser en esta sesión). `docs/modelo-datos.md` actualizado con el cambio de navegación. |
| 2026-09-02 | Sistema PSG, Fase 2 — F2 MOD 2: Alumnas | Antes de construir se detectó una discrepancia real entre lo que Groundwork 1 (20260831120000) dejó aplicado y lo que pedía este módulo — `alumnas.dni` NOT NULL UNIQUE, `alumnas.grupo_id` NOT NULL y `contactos.relacion` NOT NULL, los tres probados a propósito en esa sesión, contra un pedido de dejarlos opcionales en base para tolerar el import futuro de las ~150 alumnas de Luciana (filas incompletas, DNI repetidos). Se preguntó al usuario (pregunta de alto impacto, no se asumió en silencio): eligió relajar los 3 campos. Migración `supabase/migrations/20260902120000_f2_mod2_alumnas_ajustes_esquema.sql` aplicada contra el proyecto real (tabla `alumnas` seguía en 0 filas, sin riesgo de datos): `dni` pasa a nullable con índice único parcial `alumnas_dni_unique_idx` (`where dni is not null`, reemplaza el unique total `alumnas_dni_key`), `grupo_id` pasa a nullable (sigue obligatorio a nivel de formulario, no de base), `fecha_inscripcion` gana default `current_date`, `contactos.relacion` pasa a nullable. Verificado leyendo `information_schema.columns` antes de registrar la migración en `supabase_migrations.schema_migrations`. RLS no se tocó: Groundwork 1 ya había dejado `alumnas`/`contactos` con policies `for all` completas para Admin/Head Coach/Secretaria (contrario a lo que el MD del módulo asumía sobre un "esqueleto sin políticas" — se verificó el estado real antes de escribir código, no se asumió). **Tipos:** `Rol` (`src/types/user.ts`) suma `'Secretaria'` (todavía no es un valor real de `users.rol`, mismo criterio que la RLS de Groundwork 1 — el código de permisos ya compila contra el rol antes de que exista, sin necesitar otro cambio cuando se resuelva el parche de Dai); `Alumna`/`Contacto` (`src/types/alumna.ts`) actualizados a `dni`/`grupo_id`/`relacion` opcionales. **Navegación:** sección nueva "Administración" en `AppHeader.tsx` (no existía ningún precedente de filtro de nav por rol; se agregó un bloque condicional aparte del array `ENLACES` plano ya existente), visible solo para Admin/Head Coach/Secretaria, con un único ítem "Alumnas" por ahora. `middleware.ts` protege `/alumnas` por sesión (como el resto de las rutas); el bloqueo por rol es server-side dentro de cada página (`redirect("/dashboard")` si el rol no corresponde), mismo patrón ya usado en Tareas/Horarios — no existía precedente de gate por rol en el middleware mismo. **Listado** (`/alumnas`): sin precedente de buscador ni de filtro combinado en toda la app — se resolvió trayendo las ~150 filas completas del server (sin paginación, según el pedido) y filtrando en un client component nuevo (`AlumnasListClient.tsx`) que combina búsqueda de texto (normalizada sin mayúsculas/acentos vía `normalize("NFD")`), filtro de grupo y tabs de estado (Activas por default, clon visual de `FiltroEstadoTurnoTabs` pero con `onClick`/estado de React en vez de `Link`+query params, porque los 3 filtros se combinan en memoria) — contador "Mostrando X de Y", y las dos variantes de vacío (real vs. por filtro) que pedía el módulo. **Alta/edición** (`AlumnaForm.tsx`): sin precedente de repetidor de filas dinámicas en la app — contactos inline con `useState`, radio-like exclusivo para "pagador principal" resuelto con checkboxes controlados por React (al marcar uno se desmarcan los demás en el estado, no por constraint de base) y auto-marcado cuando queda un solo contacto (`useEffect` sobre `contactos.length`). Server actions (`alumnas/actions.ts`): `crearAlumna` (estado fijo en `'activa'`, no lo elige el formulario) y `editarAlumna` (estado editable, sincroniza contactos con un diff altas/bajas/updates contra lo ya guardado, mismo criterio conceptual que `sincronizarProfesores` de Horarios pero escrito para esta entidad). DNI duplicado valida con un `select` previo por `dni` (excluyendo la propia alumna en edición) y devuelve el apellido/nombre de la alumna existente en el mensaje de error, en vez de un error genérico de constraint. **Ficha** (`/alumnas/[id]`): página propia (no modal), mismo patrón que Horarios/Tareas; contactos listados con el pagador principal ordenado primero y con badge visual. **Verificado en el navegador** con Playwright (paquete instalado aparte en el scratchpad de la sesión, no en el proyecto — no había Playwright ni `chromium-cli` disponibles) contra las 4 cuentas reales (Admin, Head Coach, Secretaria vía su rol real `Admin` parcheado, y Profesor con Keyla): Admin/Head Coach/Secretaria ven "Administración → Alumnas" y pueden entrar; alta con 2 contactos (el primero se automarca pagador principal al quedar solo, se desmarca solo al marcar el segundo); aparece en el listado con el contador correcto; buscador encuentra "Sofía" tipeando "sofia" sin tilde; editar y cambiar de grupo persiste; dar de baja saca a la alumna del filtro "Activas" (default) y la deja en "Bajas"; reactivar la devuelve; alta con DNI ya usado rechazada con el mensaje `"Ya existe una alumna con ese DNI: Pérez Test, Sofía Test"`; un Profesor no ve "Administración" en el menú y es redirigido a `/dashboard` al pegar `/alumnas` directamente; layout mobile (iPhone 13) revisado en listado, menú, ficha y alta — colapsa a una columna, sin overflow. Sin errores de consola en ninguna de las corridas. Datos de prueba borrados al terminar (confirmado 0 filas en `alumnas`/`contactos`). `tsc --noEmit` y `next build` sin errores (sin dev server activo al buildear; se detuvo el dev server levantado para las pruebas antes de cerrar). El Personal Access Token de Supabase usado en esta sesión sigue sin revocar (ver más arriba). |
| 2026-09-02 | Sistema PSG, Fase 2 — F2 MOD 3: Pagos y cuotas | Decisiones de alcance (pago parcial permitido, recargo fijo de $10.000 como checkbox sugerido/editable, sin campo de comprobante) ya habían quedado cerradas en una conversación aparte antes de esta sesión, documentadas en el propio pedido. Antes de escribir la migración se releyó el esquema real de `alumnas`/`contactos`/`grupos` (F2 MOD 1 y 2, ya cerrados) en vez de asumir por el MD: confirmado que `alumnas.grupo_id` y `dni` son nullable (F2 MOD 2) y que `'Secretaria'` todavía no es un valor real de `users.rol` (Dai sigue parcheada en `'Admin'`). **Esquema:** migración `supabase/migrations/20260902130000_f2_mod3_pagos.sql` — tabla `pagos` (snapshot de `monto_cuota` al momento del pago, `monto_recargo` explícito por evento, `monto` = lo efectivamente cobrado en ese evento puntual, `estado` `pendiente_verificar`/`verificado`, `alumna_id` `on delete restrict` y `registrado_por` `on delete restrict` para no perder historial financiero en silencio, `contacto_id`/`verificado_por` `on delete set null`) y `pagos_metodos` (desglose por método, `on delete cascade` desde `pagos`); constraint agregado no pedido explícitamente pero de bajo riesgo, `pagos_verificacion_consistente` (`verificado_por`/`verificado_en` van siempre juntos con `estado='verificado'`), para blindar contra una corrección manual a medio hacer directo en la base (única vía de edición prevista para pagos ya verificados, fuera de alcance de la UI). RLS: política única `for all` para Admin/Head Coach/Secretaria en ambas tablas, mismo patrón que `alumnas`/`contactos` (F2 MOD 1) — `'Secretaria'` de nuevo escrita de antemano contra un rol que todavía no existe en el CHECK real. **Reglas de negocio compartidas** (`src/lib/pagos/reglas.ts`, `src/lib/pagos/saldo.ts`, `src/lib/pagos/recibo.ts`): recargo sugerido cuando ya pasó el día 10 del mes correspondiente y queda saldo sin él (checkbox editable, no un campo separado); "hoy" para estas reglas se calcula en huso horario Argentina (`America/Argentina/Buenos_Aires`, vía `toLocaleDateString("en-CA", …)`) para no correr el corte un día según dónde corra Vercel; el cálculo de saldo de una alumna+mes se comparte entre la sugerencia en vivo del formulario de alta y el reporte de Deudoras (bulk, para no hacer ~150 consultas individuales). **Backend** (`src/app/(dashboard)/pagos/actions.ts`): `crearPago` (valida métodos y los suma a `monto`, autocompleta `monto_cuota` desde `grupos.cuota_mensual` de la alumna, rechaza si la alumna no tiene grupo asignado), `marcarPagoVerificado` (única transición, arma el texto del recibo en el momento recién después de verificar, nunca antes) y `obtenerSaldoAlumnaMes` (callable directo desde cliente, no atado a un `<form>`, para la sugerencia en vivo). **Frontend:** `/pagos` (menú), `/pagos/nuevo` (buscador de alumna por apellido/nombre normalizado sin tildes — mismo criterio que el buscador de Alumnas, aunque no se compartió el componente porque uno filtra una lista completa y el otro selecciona una entre resultados; repetidor de métodos igual al patrón de contactos de `AlumnaForm`; saldo pendiente informativo antes de guardar), `/pagos/pendientes` (filtro por mes con el mismo nav prev/anterior que ya usaba Horarios — extraído a `mesAnteriorSiguiente`/`mesQuery` en `lib/utils/date.ts` como utils nuevos, sin tocar el archivo de Horarios ya cerrado; "Marcar como verificado" resuelto como botón por fila con `useTransition`, mismo patrón que `ToggleEstadoTurnoButton`, que al confirmar reemplaza el botón por el recibo con "Copiar" vía Clipboard API en el momento, sin navegar a otra página), `/pagos/recaudacion` (total + desglose por método, agregado en JS sobre filas ya traídas, mismo criterio que el resto de la app para datasets chicos) y `/pagos/deudoras` (banner de recordatorio los días 8-9 del mes actual, sin cron ni push nuevos). Sección "Administración" de `AppHeader.tsx` suma "Pagos" junto a "Alumnas"; `middleware.ts` suma `/pagos` a las rutas protegidas por sesión (el gate por rol sigue siendo server-side en cada página, mismo patrón que Alumnas). Refactor chico de paso: `AlumnasListClient.tsx` pasó a usar el `normalizarTexto` nuevo de `src/lib/utils/texto.ts` en vez de su copia local idéntica (sin cambio de comportamiento). **Verificado contra el proyecto real** (`gbnpebqcobtoegeagmcl`): 8/8 constraints (día 1 del mes, `monto_cuota`/`monto_recargo`/`monto` > 0 o >= 0, `estado` y `metodo` inválidos, `pagos_verificacion_consistente`) y RLS con los IDs reales de Admin/Head Coach/Profesor/Empleado, todo dentro de transacciones con `rollback` (0 filas de prueba restantes, confirmado con conteos antes/después) — Admin y Head Coach insertan y ven, Profesor y Empleado bloqueados en ambas operaciones. **Verificado en el navegador** con Playwright (mismo método que F2 MOD 2, instalado aparte en el scratchpad — sigue sin estar disponible `chromium-cli`) contra la única alumna real cargada hasta ahora (Juana Luna, grupo "Nivel inicial", cuota $45.000, con un contacto pagador principal real precargado por el formulario): flujo completo de pago parcial (mamá $20.000) → segundo pago completando el mes (transferencia $25.000) → verificar ambos → recibo del primero con "Saldo pendiente del mes: $25.000", recibo del segundo sin línea de saldo → Recaudación del mes suma $45.000 con desglose Efectivo/Transferencia correcto → Deudoras del mes ya no lista a la alumna; por separado, un pago cargado para un mes pasado (agosto) tildó solo el checkbox de recargo automáticamente (sin pagos verificados de ese mes y ya pasado el día 10), y Deudoras de agosto la lista con saldo $45.000 y "18 días de atraso" (hoy 2026-09-02 respecto del día 15 de agosto); Head Coach y Secretaria (Dai, rol real `Admin`) confirmados con acceso real al menú y a `/pagos`; mobile (390×844) revisado en alta y Deudoras, sin overflow. Sin errores de consola en ninguna corrida. Datos de prueba (3 pagos reales sobre Juana Luna) borrados al cerrar (confirmado 0 filas en `pagos`/`pagos_metodos`). No se probó con una cuenta Profesor/Empleado real por no tener esa contraseña a mano en esta sesión — el gate replica exactamente el patrón ya probado de Alumnas y además está reforzado por el RLS ya verificado por SQL, pero queda como hueco menor de verificación. Tampoco se simuló en vivo el banner de recordatorio de Deudoras (depende de que "hoy" sea día 8 o 9; no se forzó el reloj del sistema) — su lógica de fecha es la misma ya probada para el recargo, mismo criterio de riesgo bajo. `tsc --noEmit` y `next build` sin errores (sin dev server activo al buildear). `docs/modelo-datos.md` actualizado con las tablas `pagos`/`pagos_metodos`, el diagrama de relaciones y las reglas de negocio compartidas. El Personal Access Token de Supabase usado en esta sesión sigue sin revocar (ver más arriba). |
| 2026-09-02 | Verificación post-sesión F2 MOD 3 — Profesor/Empleado bloqueados | El usuario pasó las credenciales de Carolina (`carolina@gmail.com`, rol real `Profesor`) y Estefania (`estefania@gmail.com`, rol real `Empleado`), que faltaban al cerrar F2 MOD 3. Con Playwright, mismo método que el resto de la sesión: ninguna de las dos ve "Administración" ni "Pagos" en el menú, y pegar directo cualquiera de las 5 rutas del módulo (`/pagos`, `/pagos/nuevo`, `/pagos/pendientes`, `/pagos/recaudacion`, `/pagos/deudoras`) redirige a `/dashboard` en los dos casos. Sin cambios de código — solo cerraba un hueco de verificación. Con esto, F2 MOD 3 queda con las 4 combinaciones de rol probadas en vivo (Admin, Head Coach, Secretaria-vía-Admin con acceso; Profesor y Empleado sin acceso). |
| 2026-09-03 | Sistema PSG, Fase 2 — F2 MOD 4: Asistencia | Decisiones de alcance (marcado por defecto vacío, definición de la alerta por semanas y no por clases, edición libre de fechas pasadas) ya venían cerradas de una conversación aparte, documentadas en el pedido de la sesión. Antes de escribir la migración se releyó el esquema real de `alumnas`/`contactos` (F2 MOD 2) y `grupos`/`grupo_horarios` (Groundwork 1) en vez de asumirlo del MD: confirmado que `alumnas.grupo_id` es nullable desde F2 MOD 2, que `grupo_horarios.dias` es `smallint[]` con la convención ISO, y que `'Secretaria'` sigue sin ser un valor válido de `users.rol` (Dai parcheada en `'Admin'`). **Esquema:** migración `supabase/migrations/20260903120000_f2_mod4_asistencia.sql` — tabla `asistencia` (`alumna_id` y `registrado_por` con `on delete restrict` para no perder historial en silencio, mismo criterio que `pagos`; `grupo_id` **snapshot** `not null` del grupo en el que estaba la alumna ese día, nunca derivado de `alumnas.grupo_id` al leer; `presente` booleano sin tercer estado porque la justificación de ausencias quedó fuera de alcance), constraint `unique (alumna_id, fecha)` (una fila por alumna por día, que es lo que hace que volver a guardar una fecha actualice en vez de duplicar) y constraint `asistencia_sin_sabados` (`extract(isodow from fecha) <> 6`) — este último no estaba pedido explícitamente como constraint de base, pero el filtro de sábados sí, y se agregó como tercera capa (la UI nunca lista un sábado, la server action lo rechaza, y el check es la red de seguridad) porque el bloque de sábado de Jungla existe de verdad en `grupo_horarios` y es fácil de colar por URL. Índices compuestos `(grupo_id, fecha)` y `(alumna_id, fecha)`, uno por cada una de las dos lecturas del módulo. RLS: política única `for all` para Admin/Head Coach/Secretaria, mismo patrón que `alumnas`/`contactos` y `pagos` — `'Secretaria'` otra vez escrita de antemano contra un rol que todavía no existe en el CHECK real. **Lógica compartida** (`src/lib/asistencia/`): `fechas.ts` reusa `fechasDelMesPorDia` de Planificaciones (F2 MOD 1) y le suma el filtro de sábados, que vive acá y no en `lib/utils/date.ts` justamente porque es exclusivo de Asistencia — las planificaciones de sábado de Jungla siguen funcionando igual; `alertas.ts` calcula la alerta de inasistencias; `consultas.ts` centraliza la lectura paginada de `asistencia` (de a 1000 filas, porque una asistencia diaria de todo el club pasa el límite por defecto del API) y tira error en vez de devolver una lectura a medias, que haría que la alerta muestre menos alumnas de las que corresponde; `permisos.ts` tiene el gate de rol, fuera de `actions.ts` porque un archivo `"use server"` solo puede exportar funciones async. Refactor chico de paso: `hoyArgentina()` (huso Argentina, para no correr un día el corte según dónde corra Vercel) subió de `lib/pagos/reglas.ts` a `lib/utils/date.ts` para que Asistencia la reuse, junto con dos helpers nuevos (`sumarDias`, `lunesDeLaSemana`) — misma implementación, sin cambio de comportamiento en Pagos. **Alerta de inasistencias:** 3 semanas calendario consecutivas (lunes a domingo) sin ningún `presente`, no 3 clases sueltas — contar clases daría un umbral distinto por grupo, porque cada uno tiene 2 o 3 días de clase. La semana en curso no se cuenta (evita falsas alarmas a mitad de semana); una semana solo cuenta si tiene asistencia ya tomada, así que feriados y semanas todavía sin cargar se saltean sin sumar ni romper la racha. Dos criterios que el pedido no fijaba y se resolvieron acá, los dos hacia el lado de no inventar alertas: (a) "semana con asistencia tomada" se evalúa sobre las filas de la propia alumna y no sobre las del grupo — equivalente en el caso normal, porque el guardado en bloque crea una fila por cada alumna activa, pero evita marcar como ausente a una alumna que todavía no estaba en el grupo esas semanas; (b) si la alumna ya tuvo un presente en la semana en curso queda fuera de la alerta aunque venga de una racha de 3, porque ya volvió. Ventana de lectura de 16 semanas, de las cuales las 3 últimas deciden la alerta y el resto sirve para poder decir desde cuándo no viene. Reporte que se consulta (`/asistencia/alertas`), sin notificación ni cron — mismo criterio que "Deudoras". **UI:** `/asistencia` (menú: alertas + selector de grupo), `/asistencia/grupos/[grupoId]` (navegación por mes idéntica a la de Planificaciones, lista las fechas de clase del mes con ✓ Cargada / Pendiente / "Todavía no fue la clase" y un contador de cuántas faltan, más un aviso de confirmación al volver de un guardado), `/asistencia/grupos/[grupoId]/[fecha]` (alumnas activas ordenadas por apellido, arranca sin nadie tildado, checkbox de 20px dentro de un `<label>` de 56px de alto para el pulgar en mobile, contador vivo de presentes/ausentes, un solo submit) y `/asistencia/alertas` (nombre, grupo, semanas de racha, última vez presente y el contacto principal como link `tel:` para llamar directo). `AppHeader.tsx` suma "Asistencia" a la sección Administración y `lib/supabase/middleware.ts` suma `/asistencia` a las rutas protegidas por sesión (el gate por rol sigue siendo server-side por página, mismo patrón que Alumnas y Pagos). Tipo nuevo `src/types/asistencia.ts`, sumado a `index.ts`. **Verificación.** Esta sesión no tuvo credenciales de Supabase ni variables de entorno de la app, así que **no se aplicó la migración contra el proyecto real ni se probó en el navegador** — queda como pendiente explícito (ver punto 7). En su lugar se levantó un Postgres 16 local y se aplicó **la cadena completa de migraciones desde cero** (saltando solo las dos que necesitan `pg_cron`/`pg_net`, que no existen fuera de Supabase y no tocan Asistencia): 6/6 escenarios de esquema — guardado en bloque con presentes y ausentes en la misma operación, re-guardado de la misma fecha que actualiza sin duplicar (2 filas antes y después, valores invertidos), segunda fila para la misma alumna+fecha rechazada por el unique, sábado 2026-09-05 rechazado por el check, snapshot de grupo intacto después de mover a la alumna a otro grupo, y borrado de alumna con historial bloqueado por el `restrict` — y RLS real con `set local role authenticated` + `request.jwt.claim.sub`: Profesor ve 0 filas y su INSERT lo rechaza la policy; Head Coach ve todo, inserta y edita. La lógica de fechas y de la alerta se probó aparte con un cliente Supabase falso y datos armados relativos a la fecha de hoy: sábado excluido siempre (incluido el caso Jungla, y un grupo solo-sábado que queda sin fechas), alerta a las 3 semanas seguidas sin presente, una semana con presente rompe la racha, un feriado en el medio no la rompe ni la suma, 2 semanas no alcanzan, y un presente en la semana en curso saca de la alerta. `tsc --noEmit` y `next build` sin errores (`next lint` no está configurado en el repo — arranca el asistente interactivo de setup de ESLint, se dejó como estaba). `docs/modelo-datos.md` actualizado con la tabla `asistencia`, sus índices, el diagrama de relaciones y la definición de la alerta. **Fuera de alcance, respetado:** asistencia de sábados, baja automática por inasistencia, notificaciones/cron para la alerta, justificación de ausencias, y reportes históricos más allá de la alerta. |
| 2026-09-03 | Verificación post-sesión F2 MOD 4 — migración aplicada + `/supervisor-proyecto` | La sesión de construcción de F2 MOD 4 no tuvo credenciales de Supabase (proxy de egress bloqueaba `api.supabase.com`); esta sesión sí las tuvo. **(1) Migración aplicada:** con un Personal Access Token nuevo, se corrió `supabase/migrations/20260903120000_f2_mod4_asistencia.sql` contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API, en 4 bloques (tabla → índices → RLS → policy), sin errores. Verificado contra el catálogo real: tabla `asistencia` existe, RLS habilitado, ambos índices y la policy `asistencia_admin_headcoach_secretaria` creados. **No** se hizo el testeo de constraints/RLS con transacciones de rollback e IDs reales (método de F2 MOD 3) ni el repaso en el navegador — quedan pendientes (punto 7). **(2) Supervisor:** clonado el repo en la rama `claude/f2-mod4-asistencia-np8zf8`, confirmado working tree limpio sobre el commit `06630a0`. Verificado el diff real de ese commit contra lo que describe el log (no solo el resumen): los 19 archivos listados coinciden, el DDL de la migración aplicada coincide statement por statement con el archivo del repo, `AppHeader.tsx` suma "Asistencia" a la sección Administración, `middleware.ts` protege `/asistencia`, y las 4 páginas del módulo más `actions.ts` gatean server-side con `puedeGestionarAsistencia` (Admin/Head Coach/Secretaria — Profesor excluido). Re-corridos independientemente `tsc --noEmit` (limpio) y `next build` (limpio, confirma las 4 rutas de Asistencia en el output). Sin choques: la única otra rama remota (`claude/psg-planificaciones-ui-g3nzkf`) está vacía respecto de `main` (ya mergeada, remanente stale) y esta rama contiene todo `main` (fast-forward posible). |
| 2026-09-03 | F2 MOD 4 — constraints/RLS contra la base real (rollback) | Mismo método que F2 MOD 3: todo dentro de transacciones con `rollback`, conteos antes/después. `select count(*) from asistencia` en 0 antes de empezar. `asistencia_alumna_fecha_unica` rechaza una segunda fila para la misma alumna+fecha; `asistencia_sin_sabados` rechaza `2026-09-05` (confirmado sábado con `extract(isodow...)`); FK `alumna_id` en `restrict` confirmado insertando una alumna de prueba temporal (id fijo `00000000-...-000000000001`) con una fila de asistencia y tratando de borrarla — rechazado por `asistencia_alumna_id_fkey` (los 3 FKs de la tabla son `on delete restrict`, confirmado también leyendo `pg_constraint`). RLS simulando la sesión de cada rol real con `set local role authenticated` + `set local request.jwt.claim.sub`: Carolina (Profesor, `0dfb11aa-...`) ve 0 filas en un SELECT y su INSERT es rechazado por la policy (`42501`); Luciana (Head Coach, `572d4259-...`) y Lautaro (Admin, `0e110f60-...`) insertan y ven su propia fila. Conteo final de `asistencia` en 0, alumna de prueba temporal inexistente, y la alumna real (Juana Luna) intacta. Con esto quedan cerrados los dos pendientes de base de datos de F2 MOD 4 (punto 7) — solo queda el repaso en el navegador (los 6 puntos + mobile). |
| 2026-09-03 | F2 MOD 4 — merge a main + verificación en el navegador | El usuario prefirió revisar el módulo en producción en vez de un repaso automatizado con Playwright: se mergeó `claude/f2-mod4-asistencia-np8zf8` a `main` por fast-forward y se pusheó (`d2da79e..39846c5`), disparando el deploy en Vercel (`https://psgsystem.vercel.app`). El usuario lo probó ahí directamente y confirmó que anda todo bien, incluida la pasada en mobile. Con esto F2 MOD 4 (Asistencia) queda completamente cerrado: código, migración aplicada, constraints/RLS verificados contra la base real, y repaso en el navegador — sin pendientes propios del módulo (quedan solo los pendientes sueltos generales del punto 7, ninguno específico de Asistencia). |
| 2026-09-03 | 5 — Torneos (F2 MOD 5, último módulo de Fase 2) | Alcance recortado a solo el registro (qué alumnas participan y el control de inscripción paga quedan fuera, con su propia definición pendiente). Decisiones acordadas antes de construir: campo `tipo` (`torneo`/`exhibicion`/`evento`, default `torneo`) porque de las 7 cosas que Luciana carga a mano 2 no son torneos de competencia (Exhibición de Cierre, último entreno); el estado (Próximo/En curso/Pasado) no es columna, se calcula en cada lectura comparando fechas contra hoy (`src/lib/torneos/fechas.ts`); y **primer módulo del proyecto con permisos diferenciados** — Secretaria ve el listado pero no puede crear/editar/borrar (solo Admin y Head Coach). Migración `supabase/migrations/20260903130000_f2_mod5_torneos.sql`: tabla `torneos` (constraint `torneos_fecha_fin_valida`, índice en `fecha_inicio`, trigger `set_updated_at` reusado) y **4 policies de RLS separadas** en vez de la `for all` única que usan Pagos/Asistencia — `torneos_select_admin_headcoach_secretaria` vs. `torneos_insert/update/delete_admin_headcoach` — siguiendo el precedente ya existente en `turnos` (Head Coach con control total sobre turnos, ver 2026-08-25). Aplicada contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API en 2 bloques (tabla+índice+trigger, luego RLS+políticas), registrada en `supabase_migrations.schema_migrations`, y verificada contra el catálogo real (columnas, RLS habilitada, 4 policies con los `cmd` correctos, índice y trigger). RLS re-probada con IDs reales dentro de transacciones sin commit (autorollback al cerrar la conexión): Admin puede insertar, Head Coach ve y puede editar, Profesor (Keyla, cuenta real) no ve nada (`count = 0`) y su INSERT es rechazado por la policy (`UPDATE` afecta 0 filas) — sin dejar datos de prueba (confirmado `0` filas `TEST-RLS%` al terminar). Construido en `src/app/(dashboard)/torneos/` (listado, alta, detalle, edición, `actions.ts`) y `src/components/torneos/` (`TorneoCard`, `TorneoDestacado`, `EstadoTorneoBadge`, `FiltroAnioTorneos`, `TorneoForm`, `BorrarTorneoButton`), reusando el lenguaje visual y los patrones ya existentes (tarjetas de `TurnoCard`/`TareaCard`, gate de permisos en dos funciones separadas — `puedeVerTorneos`/`puedeGestionarTorneos`, patrón ya usado por Asistencia pero con una sola función — y confirmación nativa de borrado). Listado: tarjetas cronológicas con ícono por tipo (🏆/💫/📆), fecha en lenguaje natural nueva (`formatRangoFechasTorneo`: "7 al 9 de septiembre", "1 de noviembre", rangos que cruzan mes o año), próximo evento destacado arriba con los días que faltan, filtro por año (default el actual), pasados atenuados en vez de ocultos. Ítem "Torneos" sumado a Administración en `AppHeader.tsx` (el bloque ya estaba condicionado a Admin/Head Coach/Secretaria desde antes, sin cambios ahí) y a `RUTAS_PROTEGIDAS` en `middleware.ts`. `docs/modelo-datos.md` actualizado (tabla `torneos`, Índices, Relaciones — primera tabla de Fase 2 sin ninguna FK). Cargados los 7 eventos reales de Luciana como prueba (5 torneos, Exhibición de Cierre, último entreno — asumido año 2026), con acentos preservados (González, Río Segundo - Córdoba, Último, Exhibición) usando el método ya conocido (`Write` + `curl --data-binary`, evita la corrupción UTF-8 del shell). Probado de punta a punta con Playwright contra la cuenta real de Admin (`tsc --noEmit` y `next build` limpios primero): destacado muestra "Torneo Absoluto" con "En 4 días", los 3 formatos de fecha (un día, rango en el mes, rango que cruza mes) correctos, íconos por tipo visibles, alta/edición/borrado de un torneo de prueba (con `fecha_fin` autocompletándose al elegir `fecha_inicio`) confirmados también contra la base real, y repaso en viewport mobile (menú hamburguesa muestra "Torneos", navega bien) — sin errores de consola. No se pudo probar en el navegador con una cuenta Profesor/Empleado real ni con un rol Secretaria real (no existe todavía en la base) — ver Pendientes sueltos del punto 7. Con este módulo cierra Fase 2 completa. |
| 2026-09-03 | Corrección de alcance — Torneos: navegación y permisos de lectura | El usuario repasó el documento base y corrigió dos decisiones de la sesión anterior (mismo día, antes de cualquier commit posterior a las anteriores): (1) Torneos **no** vive en Administración — es información del calendario del club que le interesa a todos, incluidas las alumnas con login (rol Patinador/a), no un apartado de gestión interna. Movido en `AppHeader.tsx` de `ENLACES_ADMINISTRACION` a `ENLACES` (principal), ubicado arriba de "Tareas" — misma lógica que "Planificaciones", que tampoco vive en Administración. (2) El SELECT de `torneos` pasa de estar acotado a Admin/Head Coach/Secretaria a estar **abierto a cualquier autenticado** — la escritura (solo Admin/Head Coach) no cambió. Migración `supabase/migrations/20260903140000_f2_mod5_torneos_lectura_abierta.sql`: `drop policy` de la vieja `torneos_select_admin_headcoach_secretaria` + `create policy "torneos_select_authenticated" ... using (true)`, mismo patrón que `turnos_select_authenticated`. Aplicada contra el proyecto real y registrada en `supabase_migrations.schema_migrations`; verificada contra `pg_policies` (la de SELECT queda con `qual = true`, las 3 de escritura sin cambios) y reprobada con el ID real de Keyla (Profesor): antes de la corrección veía 0 filas, después ve las 7 reales, y su INSERT sigue rechazado. Nuevo: bloque de "próximo torneo" en `/dashboard` (`src/app/(dashboard)/dashboard/page.tsx`), visible para todos los roles, reusando el componente `TorneoDestacado` ya construido (no uno nuevo) — consulta el primer torneo con `fecha_fin >= hoy` (huso Argentina, `hoyArgentina()`) ordenado por `fecha_inicio`, y no renderiza nada si no hay ninguno. `puedeVerTorneos` simplificado a "cualquier rol logueado" (antes distinguía Admin/Head Coach/Secretaria). `docs/modelo-datos.md` actualizado (RLS, ubicación en nav, bloque del Dashboard). Probado con Playwright contra la cuenta real de Admin (`tsc --noEmit` y `next build` limpios primero): bloque de torneo destacado visible en el Dashboard con "Torneo Absoluto", y el menú confirma el orden `Dashboard > Torneos > Tareas > ...` con "Torneos" apareciendo antes del encabezado "Administración" — sin errores de consola. No se probó todavía en el navegador con una cuenta Profesor/Empleado/Patinador real (ver Pendientes sueltos). |
| 2026-09-08 | Parche — Import inicial de alumnas | Importadas las 158 alumnas reales del listado consolidado de Luciana (`Hoja_Definitiva_PSG`) vía script one-off `scripts/import-alumnas/import.mjs` (no vive en la app; dos modos, dry-run default y `--real` para insertar). Antes del import: borrada la alumna de prueba "Luna, Juana" y sus registros relacionados (2 `contactos`, 2 `pagos`, 1 `asistencia`) en orden por FK — `pagos`/`asistencia` son `on delete restrict` desde `alumnas` (no cascade), `contactos` sí es cascade pero se borró explícito igual. Era la única alumna cargada en la base: no había otras de prueba de MOD 2/3/4 que cuidar de no duplicar. Mapeo fijo de grupo en el script (case-insensitive): `ESCUELITA INFANTIL`/`INICIAL` → Nivel inicial, `PRE FORMATIVA` → Equipo de competencia infantil, `jungla` → Equipo de competencia (Jungla), `avanzado` → Equipo avanzado; `Recreativo adultas` no está en este CSV, carga manual aparte. Dry-run dio exactamente 158 filas a insertar (Nivel inicial 86, Equipo de competencia infantil 13, Jungla 36, Avanzado 23), 0 salteadas, 0 grupo no reconocido — coincidió con lo esperado en el MD, se corrió `--real` con confirmación del usuario. Verificado contra la base real después: 158 filas totales, conteo por grupo exacto, sin duplicados apellido+nombre, nombres con Ñ (ACUÑA, NUÑEZ, IÑIGUEZ, etc.) guardados correctamente en UTF-8. Detalle no obvio: el CSV traía dos copias de apellido/nombre (columnas 1-2 con espacios sueltos, columnas 5-6 "limpias"); a pedido del usuario el script lee las columnas 5-6, no las 1-2. `dni` NULL para las 158 (el CSV no lo tiene), sin `contactos` cargados (se completan después desde la app, alumna por alumna). Acceso a la base vía Management API con un Personal Access Token nuevo, no revocado (ver Pendientes sueltos). Sin verificación visual en el navegador en esta sesión (sin herramienta de browser disponible) — pendiente que el usuario filtre el listado de Alumnas por los 4 grupos en la app y confirme los conteos de arriba. |
| 2026-09-08 | Correcciones pre-UI — Bloque 1 (Seguridad) | Cerrados los cuatro agujeros de permisos previos a la UI nueva. **1.1** `actualizarEstadoTarea` era la única action del módulo sin validación: recibía `id` + estado y escribía, así que cualquier autenticado (incluida una Patinadora) podía completar cualquier tarea del club. Ahora carga el perfil y la tarea (`created_by` + `tarea_asignados`) y aplica: Admin/Head Coach/Secretaria cualquier tarea, Profesor/Empleado solo donde figuran asignados, Patinador nunca. **1.2** La regla de permisos de Tareas quedó centralizada en `src/lib/permisos.ts` (`puedeVerModuloTareas`, `puedeCrearTarea`, `puedeEditarTarea`, `puedeCambiarEstadoTarea`, más `puedeVerModuloMiembros`/`puedeVerEmailsMiembros`) y la consumen tanto las pages como las actions: antes el botón "Nueva tarea" y `crearTarea` no coincidían (el botón excluía a Secretaria, la action la dejaba pasar) y `editarTarea` dejaba escribir a cualquiera que no fuera Empleado ni Patinador, sin verificar autor/responsable como sí exigía la pantalla. `editarTarea` ahora lee la tarea y valida antes del update. **1.3** `/tareas`, `/tareas/[id]` y `/tareas/[id]/editar` estaban abiertos a cualquier autenticado: ahora redirigen a `/dashboard` para Patinador. `/miembros` idem, y además el email del personal se devuelve solo para Admin/Head Coach/Secretaria (se saca de la propia query, no se oculta por CSS). **1.4** `NotificacionesBell` no filtraba por usuario en el `select` de las últimas 20 ni en el `count` de no leídas (confiaba solo en la RLS, mientras el canal de realtime sí filtraba): se agregó `.eq("usuario_id", usuarioId)` a las dos. Typecheck `tsc --noEmit` limpio; el proyecto no tiene ESLint configurado (`next lint` pide setup interactivo, no se corrió). Sin verificación en el navegador todavía. |
| 2026-09-08 | Correcciones pre-UI — Bloque 3 (Bug de fecha) | Un solo criterio de fecha en toda la app: `hoyArgentina()` (`src/lib/utils/date.ts`). El Dashboard calculaba `hoyStr` con `new Date().toISOString()` (UTC) para tareas y turnos mientras el torneo destacado ya usaba `hoyArgentina()`: entre las 21:00 y la medianoche de Argentina, UTC ya está en el día siguiente, así que las clases que estaban por empezar desaparecían del listado y "Clases hoy" contaba las de mañana — justo la franja en la que se usa la app. `hoyStr` pasa a ser `hoyArgentina()` (y el bloque de torneo reusa esa misma variable, se eliminó el `hoyArg` duplicado); la ventana de 7 días ahora es `sumarDias(hoyStr, 7)` en vez de aritmética sobre un `Date` local. Además, encontrados fuera del punto pero del mismo criterio: `AlumnaForm` proponía como fecha de inscripción por defecto el `toISOString()` del navegador, y **5 pantallas derivaban el mes por defecto de `new Date()` del servidor** (Pagos pendientes/recaudación/deudoras y las dos de Horarios por grupo) — mismo desfase, pero disparándose el último día del mes: a las 21:00 del 30 el selector ya abría en el mes siguiente. Se unificaron con el helper nuevo `anioMesDeHoy()`. Los dos `toISOString()` que quedan en el repo son correctos y se dejaron: `verificado_en` (timestamp completo) y `sumarDias` (construido en UTC a propósito). Typecheck limpio. |
| 2026-09-08 | Correcciones pre-UI — Bloque 2 (Rol Secretaria) | **Aplicado y verificado contra el proyecto real** (`gbnpebqcobtoegeagmcl`, Management API), migración `20260908120000_rol_secretaria.sql` registrada en `supabase_migrations.schema_migrations`. `users_rol_check` pasa a aceptar `'Secretaria'`; **Dai quedó con rol `Secretaria` y cargo `Administración`** (antes `Admin` + cargo `Secretaria`). `grupo_objetivos_mes_write` deja de nombrarla: Planificaciones queda en solo lectura, verificado en vivo con rollback (su INSERT lo rechaza la RLS con 42501). **Hallazgo del repaso de 2.3, releyendo `pg_policies` contra la base y no las migraciones del repo: ninguna de las 9 policies de Tareas nombraba a `'Secretaria'`** — se habían escrito antes de que el rol existiera. Migrar a Dai sin tocarlas le vaciaba el módulo en silencio (probado con rollback: veía 0 tareas y no podía crear). Se recrearon las 9 de `tareas`/`tarea_asignados`/`tarea_comentarios`. **Dos decisiones del usuario en esta sesión, porque el documento se contradecía consigo mismo o con la RLS viva:** (1) 2.2 decía Tareas «Como Head Coach» pero el detalle «Ve todas», y no son lo mismo — con el criterio de Head Coach la única tarea cargada hoy (creada por Luciana, sin asignados) le desaparecía a Dai; ganó «ve todas», así que Secretaria tiene SELECT completo de tareas como Admin, y sigue creando/borrando solo las propias. (2) El punto 1.1 daba a Head Coach y Secretaria el cambio de estado de cualquier tarea, pero la RLS solo dejaba tocar las propias o asignadas: el update se rechazaba sin error y la pantalla decía que había andado. Ganó el 1.1: se amplió `tareas_update_admin_profesor_headcoach` (Admin y Secretaria cualquiera; Head Coach cualquiera de las que ve; Profesor sigue en propias/asignadas). Contrapartida asumida: la RLS es por fila, no por columna, así que a nivel base también pueden editar título/fechas ajenos — la app lo sigue bloqueando en `puedeEditarTarea`. (3) Los contadores del Dashboard, que eran solo Admin y no figuraban en 2.2, se abrieron a Head Coach y Secretaria. Resto del repaso: `alumnas`/`contactos`/`pagos`/`pagos_metodos`/`asistencia` y el SELECT de `torneos` ya la nombraban y arrancaron solas (verificado: como Secretaria ve las 158 alumnas y los 7 torneos); `turnos`/`grupos`/`grupo_horarios` y `users_update_admin`/`users_delete_admin` no la nombran, que es lo correcto. Código: `/pagos/recaudacion` deja de aceptarla (gate + tarjeta oculta en `/pagos`) vía `puedeVerRecaudacion()`; limpiados los comentarios de parche de `src/types/user.ts` y de las 4 pantallas/actions de horarios. Typecheck limpio. **Falta la prueba en el navegador con la cuenta real de Dai** (ver Pendientes sueltos). |
| 2026-09-08 | Correcciones pre-UI — Bloque 4 (Campos que faltaban) | **Aplicado y verificado contra el proyecto real**, migración `20260908130000_bloque4_campos.sql` registrada en `schema_migrations`. (1) `alumnas.fecha_nacimiento` `date` nullable — las 158 fichas ya cargadas no tienen el dato, donde falte se muestra vacío; sumada al formulario junto al DNI y al detalle de la alumna. (2) **Baja lógica de personal**: `users.estado` (`'activo'`/`'baja'`, default `'activo'`), mismo criterio que `alumnas.estado` pero en masculino porque el listado es de personal mixto. Antes, si alguien se iba había que borrar la fila (perdiendo sus tareas y turnos históricos, o chocando contra los FK `on delete restrict`) o dejarla activa para siempre. Ya filtran por él: `/miembros`, los selectores de responsables de Tareas (alta y edición) y los de profesores de Horarios/Planificaciones. **No hay pantalla para dar de baja**: la app no tiene gestión de usuarios (nadie invita ni cambia roles desde la UI), así que por ahora la baja se carga a mano en la base. (3) `pagos.recibo_texto`: el texto de WhatsApp se armaba al verificar, se mostraba una vez y se perdía al salir; ahora se guarda en el pago al verificarlo. Decisión del usuario: campo en `pagos` y no tabla aparte — no hace falta histórico de envíos. La pantalla para reenviarlo queda para la UI nueva. (4) **Corrección de pagos**: decisión del usuario, la transición nueva es *anular con motivo obligatorio* (ni editar ni borrar ni rechazar). `pagos.estado` acepta `'anulado'` + `anulado_por`/`anulado_en`/`motivo_anulacion`; action `anularPago` y botón en Pendientes de verificar. El CHECK `pagos_verificacion_consistente` tenía solo dos ramas y cualquier fila anulada lo violaba: se le agregó la rama de anulación, que exige los tres campos juntos y conserva `verificado_por`/`verificado_en` si el pago ya estaba verificado. Los anulados salen solos de todo cálculo de plata (saldo, deudoras y recaudación filtran por `'verificado'`). Verificado en vivo con rollback: anular sin motivo lo rechaza el CHECK (23514), con motivo pasa, y no quedaron filas de prueba (`pagos` sigue en 0). Typecheck limpio. |
| 2026-09-08 | Correcciones pre-UI — Bloque 5 (Participación en torneos) | **Solo el modelo, aplicado y verificado contra el proyecto real** (migración `20260908140000_torneo_participantes.sql`, registrada en `schema_migrations`). Las tres pantallas del recorrido son parte de la UI nueva: la Definition of done del documento dice explícitamente «esto no cambia ninguna pantalla» y no lista al Bloque 5 entre sus ítems. Tabla `torneo_participantes` (una fila por alumna convocada, única por `(torneo_id, alumna_id)`, índices en ambas FK) con `categoria` **texto libre sin enum** —las categorías federativas son demasiadas y varían por torneo y federación, así que vive en la convocatoria y no en la ficha de la alumna; el eje de búsqueda para convocar es `alumnas.grupo_id`, que ya existía—, `inscripcion_estado` (`Pendiente`/`Paga`/`Exenta`), `inscripcion_monto`, `pago_id` (une la inscripción con el pago real), `convocada_por` y `creado_en`. `alumna_id` en `on delete restrict` para no perder el histórico; `torneo_id` en `cascade`. Sumada también `torneos.inscripcion_monto` para precargar. **RLS verificada en vivo, rol por rol, con transacciones con rollback** (sin filas residuales): Head Coach convoca ✅; Profesora lee la lista ✅ pero su INSERT es rechazado (42501) ✅; Secretaria cambia el estado de inscripción ✅ pero no puede convocar (42501) ✅; Empleada ve 0 filas ✅. Detalle no obvio de la primera prueba: el INSERT de la Profesora dio *éxito con 0 filas* en vez de error, porque el `select` que le daba origen leía `alumnas`, que ella no ve — la prueba se repitió con ids literales y ahí sí saltó la RLS. **Dos límites del alcance no se pueden expresar en la RLS** (es por fila, no por columna, y las column privileges de Postgres son por rol de base y no por usuario de la app): que la Profesora vea la lista sin monto ni estado de pago, y que `categoria` la escriban solo Admin/Head Coach. Quedan del lado de la aplicación, mismo criterio que el email del personal en `/miembros`; anotado en `docs/modelo-datos.md`. Tipos `TorneoParticipante`/`EstadoInscripcion` agregados. Typecheck limpio. |
| 2026-09-08 | Correcciones pre-UI — Pagos en `permisos.ts` (cierre del Bloque 1.2) | Solo código, sin tocar la base. El Bloque 1.2 mandaba dejar la regla de permisos escrita en un solo lugar, pero se había aplicado únicamente a Tareas: en Pagos la regla seguía en un helper local de `pagos/actions.ts` y **la misma condición estaba repetida literal en las cuatro pantallas** (`/pagos`, `/pagos/nuevo`, `/pagos/pendientes`, `/pagos/deudoras`). Detectado al pedir el usuario confirmación de tres detalles del módulo. Ahora viven en `src/lib/permisos.ts`: `puedeGestionarPagos` (entrar, registrar, verificar — Admin/Head Coach/Secretaria), `puedeAnularPago` (misma terna, pero aparte por ser una transición destructiva: si algún día se acota, se acota en un solo lugar) y `puedeVerRecaudacion` (Admin/Head Coach, la excepción del módulo). Sin cambio de comportamiento para ningún rol: los helpers devuelven exactamente los mismos roles que las condiciones que reemplazan. Detalle no obvio: al pasar el gate de `!profile || rol !== ...` al helper, TypeScript dejó de estrechar `profile` a no-nulo y `/pagos` rompía al usarlo después para ocultar la tarjeta de Recaudación — resuelto con `profile?.rol`. **Quedan dos transiciones de pago a propósito** (verificar y anular con motivo): el usuario descartó explícitamente sumar editar y rechazar — la anulación ya resuelve la corrección (se anula y se carga de nuevo) y no justifica una migración y un estado nuevo para llegar al mismo lugar. Typecheck y `next build` limpios. |

---

## Pendientes cerrados en este mes

> Trasladados tal cual desde la sección "Pendientes sueltos" de `PROGRESS.md`, que los llevaba tachados. Los pendientes todavía abiertos viven en `docs/pendientes.md`.

- ~~F2 MOD 5 (Torneos): no se pudo probar con una cuenta real de rol `'Secretaria'`~~ — **el rol ya existe desde el 2026-09-08** y Dai está migrada; la prueba en el navegador con su cuenta sigue pendiente (ver el pendiente nuevo de abajo). Texto original: no se pudo probar porque ese valor todavía no existe en `users.rol` (Dai sigue en `Admin`, ver punto 2) — ya no es relevante para el SELECT (abierto a cualquier autenticado desde la corrección), pero sigue sin probarse en vivo que a Dai (si se la migrara a `'Secretaria'`) se le ocultarían los botones de gestión.
- ~~Import del listado real de ~150 alumnas — sesión aparte, cuando Luciana entregue el archivo.~~ — **cerrado el 2026-09-08**: importadas las 158 alumnas reales vía `scripts/import-alumnas/import.mjs`, ver Log. No se probó visualmente en el navegador en esa sesión (sin herramienta de browser disponible) — pendiente que el usuario filtre el listado de Alumnas por los 4 grupos en la app y confirme: Nivel inicial 86, Equipo de competencia infantil 13, Equipo de competencia (Jungla) 36, Equipo avanzado 23.
- ~~F2 MOD 4 (Asistencia): la migración `20260903120000_f2_mod4_asistencia.sql` todavía NO se aplicó contra el proyecto real de Supabase~~ — **cerrado el 2026-09-03**: aplicada contra el proyecto real (`gbnpebqcobtoegeagmcl`) vía Management API, en 4 bloques secuenciales (tabla → índices → RLS → policy), todos sin error. Verificado directamente contra el catálogo real: tabla `asistencia` existe, RLS habilitado, ambos índices (`asistencia_grupo_id_fecha_idx`, `asistencia_alumna_id_fecha_idx`) y la policy `asistencia_admin_headcoach_secretaria` creados.
- ~~F2 MOD 4 (Asistencia): constraints/RLS sin probar contra la base real con IDs reales~~ — **cerrado el 2026-09-03**: mismo método que F2 MOD 3, todo dentro de transacciones con `rollback` y conteos antes/después (0 filas de prueba restantes, alumna real Juana Luna intacta). `asistencia_alumna_fecha_unica` y `asistencia_sin_sabados` (probado con 2026-09-05, confirmado sábado) rechazan como corresponde; FK `alumna_id` en `restrict` confirmado con una alumna de prueba temporal (los 3 FKs de la tabla son `on delete restrict`, confirmado también por catálogo). RLS con los IDs reales de Carolina (Profesor), Luciana (Head Coach) y Lautaro (Admin): Profesor ve 0 filas y su INSERT es rechazado por la policy; Head Coach y Admin insertan y ven.
- ~~F2 MOD 4 (Asistencia): sin verificar en el navegador~~ — **cerrado el 2026-09-03**: la rama se mergeó a `main` y se desplegó en `https://psgsystem.vercel.app` (fast-forward `d2da79e..39846c5..b164b13`); el usuario lo probó ahí directamente (en vez de un repaso con Playwright) y confirmó que anda todo bien, incluida la pasada en mobile.
- ~~F2 MOD 3 (Pagos): no se probó con una cuenta Profesor/Empleado real~~ — **cerrado el 2026-09-02**: el usuario pasó las credenciales de Carolina (Profesor) y Estefania (Empleado) y se confirmó con Playwright que ninguna de las dos ve "Administración"/"Pagos" en el menú, y que las 5 rutas de Pagos (`/pagos`, `/pagos/nuevo`, `/pagos/pendientes`, `/pagos/recaudacion`, `/pagos/deudoras`) redirigen a `/dashboard` al pegarlas directo.
- ~~Decisión pendiente sin resolver (mencionada en el punto 2): el rol `'Secretaria'` para Dai sigue sin nombre ni alcance definido.~~ — **cerrado el 2026-09-08**: rol creado, alcance definido y Dai migrada (Correcciones pre-UI, Bloque 2). Se confirmó lo que anticipaba esta nota (las policies de `alumnas`/`contactos`/`pagos`/`pagos_metodos`/`asistencia` arrancaron solas, sin migración de permisos), pero **no** alcanzó con reasignar el rol: ninguna policy de Tareas nombraba a `'Secretaria'` y hubo que recrear las 9. Texto original: La RLS de `alumnas`/`contactos` (Fase 2, Sesión 1), la de `pagos`/`pagos_metodos` (F2 MOD 3) y la de `asistencia` (F2 MOD 4) ya quedaron escritas contra ese rol de antemano, así que en cuanto se decida y se agregue `'Secretaria'` al CHECK de `users.rol`, no hace falta otra migración de permisos para esas tablas — solo reasignar el rol de Dai.

## Estado al cerrar el mes

> Texto trasladado tal cual desde la sección "Estado actual" de `PROGRESS.md`. El estado vigente de cada módulo vive hoy en la tabla de `PROGRESS.md`; el plan por fases, en `docs/roadmap-general.md`.

**Módulo activo:** ninguno. Última sesión: **Correcciones pre-UI, 2026-09-08** — los 5 bloques del documento de correcciones, aplicados y verificados contra el proyecto real, un commit por bloque. Arreglos del sistema actual antes de empezar la UI nueva, que se escribe encima de estas reglas: **Bloque 1** (seguridad: `actualizarEstadoTarea` no validaba nada y cualquier autenticado podía completar cualquier tarea del club; regla de permisos centralizada en `src/lib/permisos.ts`; `/tareas` y `/miembros` cerrados a Patinador; email del personal solo para Admin/Head Coach/Secretaria; notificaciones filtradas por usuario). **Bloque 2** (rol `Secretaria` creado y Dai migrada, ver punto 2). **Bloque 3** (un solo criterio de fecha, `hoyArgentina()`: el Dashboard usaba UTC y entre las 21:00 y la medianoche argentina las clases del día desaparecían del listado). **Bloque 4** (`alumnas.fecha_nacimiento`, baja lógica de personal en `users.estado`, recibo de pago guardado, anulación de pagos con motivo). **Bloque 5** (modelo de `torneo_participantes`, sin pantallas). `docs/modelo-datos.md` actualizado con los cinco.
**Próximo paso:** **la UI nueva.** Es lo que venía esperando a estas correcciones. Lo primero que le toca encima: las tres pantallas de participación en torneos (el modelo ya está, ver Bloque 5) y la pantalla para reenviar el recibo de un pago (el campo ya está, ver Bloque 4). Antes de eso, la verificación en el navegador que quedó pendiente de esta sesión (ver Pendientes sueltos).
